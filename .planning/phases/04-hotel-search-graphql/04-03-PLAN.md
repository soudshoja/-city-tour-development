---
phase: 04-hotel-search-graphql
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
  - database/migrations/2026_02_21_100001_create_company_dotw_credentials_table.php
  - app/Models/CompanyDotwCredential.php
  - app/GraphQL/Queries/DotwSearchHotels.php
autonomous: true
gap_closure: true
requirements: [SEARCH-03, SEARCH-06, B2B-01, B2B-02, B2B-03]

must_haves:
  truths:
    - "REQUIREMENTS.md marks SEARCH-06 as unchecked ([ ]) with a deferral note pointing to Phase 5 getRoomRates"
    - "searchHotels without a currency field uses the company's configured currency (not hardcoded USD)"
    - "CompanyDotwCredential model has a currency attribute defaulting to USD, readable from the credentials row"
    - "DotwSearchHotels resolver reads currency from CompanyDotwCredential when input.currency is absent"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "SEARCH-06 unchecked, moved to Phase 5 with deferral note; SEARCH-03 updated to reflect DB-backed company currency default"
    - path: "database/migrations/2026_02_21_100001_create_company_dotw_credentials_table.php"
      provides: "currency column (string, default USD) added to company_dotw_credentials table"
    - path: "app/Models/CompanyDotwCredential.php"
      provides: "currency in $fillable and $casts; PHPDoc updated"
    - path: "app/GraphQL/Queries/DotwSearchHotels.php"
      provides: "currency resolved from CompanyDotwCredential when input.currency is absent"
  key_links:
    - from: "app/GraphQL/Queries/DotwSearchHotels.php"
      to: "app/Models/CompanyDotwCredential.php"
      via: "CompanyDotwCredential::where('company_id', $companyId)->value('currency') ?? 'USD'"
      pattern: "CompanyDotwCredential.*currency"
    - from: ".planning/REQUIREMENTS.md"
      to: "Phase 5 RATE-02 description"
      via: "SEARCH-06 deferral note in requirements text"
      pattern: "SEARCH-06.*Phase 5"
---

<objective>
Close two gaps found during Phase 4 verification: (1) REQUIREMENTS.md incorrectly marks SEARCH-06 as complete — hotel metadata (name, city, rating, location, image_url) is intentionally deferred to Phase 5 getRoomRates; (2) SEARCH-03 requires currency to default to company-configured currency, but the implementation hardcodes "USD" because no currency column existed on company_dotw_credentials.

Purpose: Bring requirement tracking and implementation into alignment so Phase 5 planning starts from accurate state.
Output: Updated REQUIREMENTS.md (SEARCH-06 unchecked + deferral note), currency column in migration + model, and company-currency lookup in DotwSearchHotels resolver.
</objective>

<execution_context>
@/home/soudshoja/.claude/get-shit-done/workflows/execute-plan.md
@/home/soudshoja/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-hotel-search-graphql/04-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix REQUIREMENTS.md — uncheck SEARCH-06 and annotate SEARCH-03</name>
  <files>.planning/REQUIREMENTS.md</files>
  <action>
    Two requirement tracking corrections:

    **SEARCH-06 fix:**
    Find the line:
    ```
    - [x] **SEARCH-06**: Response includes hotel code, name, city, rating, location, image_url, cheapest rates grouped by room type
    ```
    Change to:
    ```
    - [ ] **SEARCH-06**: Response includes hotel code, name, city, rating, location, image_url, cheapest rates grouped by room type — *hotel_code and cheapest rates satisfied in Phase 4; name, city, rating, location, image_url deferred to Phase 5 getRoomRates (DOTW searchhotels command does not return hotel metadata)*
    ```

    **SEARCH-03 fix:**
    Find the line:
    ```
    - [x] **SEARCH-03**: Query accepts currency (defaults to company currency if not specified)
    ```
    Change to:
    ```
    - [x] **SEARCH-03**: Query accepts currency (defaults to company-configured currency stored in company_dotw_credentials.currency, fallback USD) — *Phase 4 gap closure adds currency column to credentials and resolves it in DotwSearchHotels*
    ```

    Do NOT change any other lines. Do not alter Phase 5 section — SEARCH-06 metadata fields will be addressed in Phase 5 planning as part of getRoomRates output.
  </action>
  <verify>
    grep "SEARCH-06" .planning/REQUIREMENTS.md
    grep "SEARCH-03" .planning/REQUIREMENTS.md
    Expected: SEARCH-06 shows "- [ ]" (unchecked) with deferral note. SEARCH-03 shows "- [x]" with updated description referencing company_dotw_credentials.currency.
  </verify>
  <done>SEARCH-06 is unchecked in REQUIREMENTS.md with a clear deferral note. SEARCH-03 is checked with updated description matching the implementation.</done>
</task>

<task type="auto">
  <name>Task 2: Add currency column to company_dotw_credentials migration and model</name>
  <files>
    database/migrations/2026_02_21_100001_create_company_dotw_credentials_table.php
    app/Models/CompanyDotwCredential.php
  </files>
  <action>
    **Migration change:**
    In `database/migrations/2026_02_21_100001_create_company_dotw_credentials_table.php`, inside the `Schema::create` closure, add a currency column directly after the `markup_percent` line:
    ```php
    $table->string('currency', 3)->default('USD');
    ```
    Place it between the `markup_percent` line and the `is_active` line so the schema reads:
    ```php
    $table->decimal('markup_percent', 5, 2)->default(20.00);
    $table->string('currency', 3)->default('USD');
    $table->boolean('is_active')->default(true);
    ```

    **Model change (`app/Models/CompanyDotwCredential.php`):**
    1. Add `'currency'` to the `$fillable` array (after `'markup_percent'`).
    2. Add `'currency' => 'string'` to the `$casts` array (after `'markup_percent'` cast).
    3. Update the class-level PHPDoc `@property` block to add: `* @property string $currency ISO 4217 currency code for this company (default USD)`

    Do NOT add an encrypted accessor for currency — it is not sensitive. Plain string column.

    Run Pint after editing the model:
    ```bash
    ./vendor/bin/pint app/Models/CompanyDotwCredential.php
    ```
  </action>
  <verify>
    grep "currency" database/migrations/2026_02_21_100001_create_company_dotw_credentials_table.php
    grep "currency" app/Models/CompanyDotwCredential.php
    ./vendor/bin/pint app/Models/CompanyDotwCredential.php --test
    Expected: migration has `$table->string('currency', 3)->default('USD')`, model has currency in fillable and casts, Pint passes with no changes.
  </verify>
  <done>currency column exists in migration with default 'USD'. CompanyDotwCredential model has currency in $fillable, $casts, and PHPDoc.</done>
</task>

<task type="auto">
  <name>Task 3: Resolve company currency in DotwSearchHotels instead of hardcoded USD</name>
  <files>app/GraphQL/Queries/DotwSearchHotels.php</files>
  <action>
    In `app/GraphQL/Queries/DotwSearchHotels.php`, replace the hardcoded USD currency fallback with a DB lookup from `CompanyDotwCredential`.

    **Step 1 — Add import at top of file** (after the existing `use` statements):
    ```php
    use App\Models\CompanyDotwCredential;
    ```

    **Step 2 — Replace the currency line** (currently line ~81):
    Change:
    ```php
    $currency = trim($input['currency'] ?? 'USD');
    ```
    To:
    ```php
    $currency = trim($input['currency'] ?? '') ?: (CompanyDotwCredential::where('company_id', $companyId)->value('currency') ?? 'USD');
    ```

    This reads: if `input.currency` is provided and non-empty, use it; otherwise look up the company's configured currency from the DB; if no row exists yet, fall back to 'USD'.

    Run Pint after editing:
    ```bash
    ./vendor/bin/pint app/GraphQL/Queries/DotwSearchHotels.php
    ```

    Run PHP syntax check:
    ```bash
    php -l app/GraphQL/Queries/DotwSearchHotels.php
    ```
  </action>
  <verify>
    grep -n "CompanyDotwCredential\|currency" app/GraphQL/Queries/DotwSearchHotels.php
    ./vendor/bin/pint app/GraphQL/Queries/DotwSearchHotels.php --test
    php -l app/GraphQL/Queries/DotwSearchHotels.php
    Expected: CompanyDotwCredential imported, currency line uses DB lookup with 'USD' fallback, Pint clean, no PHP syntax errors.
  </verify>
  <done>DotwSearchHotels resolver reads company currency from CompanyDotwCredential when input.currency is not provided. Hardcoded 'USD' fallback is preserved only as last resort when no credentials row exists.</done>
</task>

</tasks>

<verification>
After all three tasks:
1. `grep "SEARCH-06" .planning/REQUIREMENTS.md` — shows `[ ]` unchecked with deferral note
2. `grep "SEARCH-03" .planning/REQUIREMENTS.md` — shows `[x]` with updated description
3. `grep "currency" database/migrations/2026_02_21_100001_create_company_dotw_credentials_table.php` — shows `$table->string('currency', 3)->default('USD')`
4. `grep "currency" app/Models/CompanyDotwCredential.php` — appears in fillable, casts, and PHPDoc
5. `grep "CompanyDotwCredential" app/GraphQL/Queries/DotwSearchHotels.php` — shows import and usage
6. `./vendor/bin/pint app/GraphQL/Queries/DotwSearchHotels.php --test` — passes
7. `php -l app/GraphQL/Queries/DotwSearchHotels.php` — no syntax errors
</verification>

<success_criteria>
- REQUIREMENTS.md: SEARCH-06 unchecked ([ ]) with deferral note to Phase 5 getRoomRates
- REQUIREMENTS.md: SEARCH-03 checked ([x]) with accurate description of company currency lookup
- company_dotw_credentials migration has currency column (string(3), default USD)
- CompanyDotwCredential model has currency in $fillable, $casts
- DotwSearchHotels resolver uses DB-backed company currency when input.currency is absent
- All files pass Pint and PHP syntax check
- No test regressions (php artisan test)
</success_criteria>

<output>
After completion, create `.planning/phases/04-hotel-search-graphql/04-03-SUMMARY.md` with:
- Tasks completed
- Commit hashes
- Decisions made
- Gaps closed (SEARCH-06, SEARCH-03)
</output>
