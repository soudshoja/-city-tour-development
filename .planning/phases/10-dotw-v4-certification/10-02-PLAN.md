---
phase: 10-dotw-v4-certification
plan: "02"
type: execute
wave: 2
depends_on:
  - "10-01"
files_modified:
  - app/Console/Commands/DotwCertify.php
autonomous: true
requirements:
  - CERT-14
  - CERT-15
  - CERT-16
  - CERT-17
  - CERT-18
  - CERT-19
  - CERT-20

must_haves:
  truths:
    - "php artisan dotw:certify --test=14,15,16,17,18,19,20 completes without PHP fatal or ManuallyFailedException"
    - "Tests 14-20 all produce PASS or WARN in storage/logs/dotw_certification.log (never ERROR)"
    - "WARN outcomes are acceptable and expected when dev environment lacks APR/changedOccupancy/restricted/minStay/promotions/propertyFees rate types"
    - "No test exits silently — every test 14-20 has an endTest() call on every exit path"
  artifacts:
    - path: "app/Console/Commands/DotwCertify.php"
      provides: "Tests 14-20 with confirmed PASS/WARN verdicts from live run"
      contains: "runTest14"
  key_links:
    - from: "runTest14-20 WARN paths"
      to: "endTest(N, true)"
      via: "every conditional exit calls endTest before return"
      pattern: "endTest\\(1[4-9]|endTest\\(20"
    - from: "runTest16 APR path"
      to: "savebooking + bookitinerary XML"
      via: "nonrefundable=yes detection branches to savebooking flow"
      pattern: "nonrefundable"
---

<objective>
Run tests 14-20 live against xmldev.dotwconnect.com (now bug-free after plan 10-01) and fix any runtime failures discovered. These tests cover changed occupancy, special promotions, APR booking, restricted cancellation, minimum stay, special requests, and property taxes/fees.

Purpose: Tests 14-20 handle edge-case rate conditions. The WARN pattern is correct when dev environment lacks a rate type — PASS is still the verdict. Any actual code logic failures must be fixed.

Output: Tests 14-20 all producing PASS or WARN in the certification log, with no test crashing or producing ERROR.
</objective>

<execution_context>
@/home/soudshoja/.claude/get-shit-done/workflows/execute-plan.md
@/home/soudshoja/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-dotw-v4-certification/10-RESEARCH.md
@.planning/phases/10-dotw-v4-certification/10-01-SUMMARY.md
@app/Console/Commands/DotwCertify.php
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run tests 14-20 live and fix any runtime failures</name>
  <files>app/Console/Commands/DotwCertify.php</files>
  <action>
    Run tests 14-20 against the live DOTW dev environment:

    ```bash
    cd /home/soudshoja/soud-laravel
    php artisan dotw:certify --test=14,15,16,17,18,19,20
    ```

    Review `storage/logs/dotw_certification.log`. For each of the 7 tests, find the verdict. Expected outcomes:

    TEST 14 (changed occupancy):
    - If DOTW returns a rate with `changedOccupancy` element: full flow should execute (blockRates → confirmbooking with validForOccupancy values)
    - If no changedOccupancy rate found: WARN + endTest(14, true) — this is acceptable
    - FAIL signal: code crashes on XML navigation, or validForOccupancy values wrong in confirmbooking XML

    TEST 15 (special promotions / specialsApplied):
    - If rate has specialsApplied: must log each special promotion name
    - If not found in dev env: WARN + endTest(15, true) — acceptable
    - FAIL signal: wrong XML path for specialsApplied attribute/element

    TEST 16 (APR — savebooking+bookitinerary):
    - If rate has `nonrefundable="yes"`: full savebooking+bookitinerary flow must run
    - If not found: WARN + endTest(16, true) — acceptable
    - FAIL signal: APR detection not triggering, or savebooking XML malformed

    TEST 17 (restricted cancellation):
    - If rate has `cancelRestricted="true"` or `amendRestricted="true"`: pass verification
    - If not found: WARN + endTest(17, true) — acceptable
    - FAIL signal: wrong attribute access for cancelRestricted

    TEST 18 (minimum stay / dateApplyMinStay):
    - If rate has `dateApplyMinStay`: log the date and pass
    - If not found: WARN + endTest(18, true) — acceptable
    - FAIL signal: wrong path to dateApplyMinStay element/attribute

    TEST 19 (special requests with DOTW internal code):
    - Must send confirmbooking XML with `<specialRequest>` element containing correct DOTW code
    - FAIL signal: XML body missing specialRequest element, or wrong code format

    TEST 20 (property taxes/fees):
    - Path now fixed to rateBasis->propertyFees (from plan 10-01)
    - If fees found: log each fee with includedinprice Yes/No
    - If not found: WARN + endTest(20, true) — acceptable
    - FAIL signal: navigation error, or includedinprice not being read as attribute

    For each FAIL:
    1. Read the full test section in the log (request XML, response XML, assertions)
    2. Identify root cause (XML path, attribute vs element, missing field)
    3. Fix in DotwCertify.php using the established scaffold patterns:
       - `$this->buildRequest()` for XML construction
       - `$this->post()` for requests
       - `$this->assertSuccess()` for success check
       - `$this->pass()` / `$this->failStep()` / `$this->warn()` for outcomes
       - `$this->endTest(N, true/false)` to record final verdict
    4. Re-run the single test: `php artisan dotw:certify --test=N`
    5. Verify PASS or WARN in log

    Key check — every exit path in tests 14-20 must call `endTest()`. Scan each method to confirm no `return;` statement exits without first calling `endTest()`. Missing endTest() would cause the test to not appear in the final summary.

    After all 7 tests are green, run the batch one final time to confirm:
    ```bash
    php artisan dotw:certify --test=14,15,16,17,18,19,20
    ```

    Run `./vendor/bin/pint app/Console/Commands/DotwCertify.php` after all fixes.
    Run `./vendor/bin/phpstan analyse app/Console/Commands/DotwCertify.php --level=5` — fix any errors.
  </action>
  <verify>
    php artisan dotw:certify --test=14,15,16,17,18,19,20 ; echo "Exit: $?"
    Expected: clean exit (no PHP fatal, no exception trace), exit code 0

    cat storage/logs/dotw_certification.log | grep -E "TEST (1[4-9]|20).*:" | grep -v "PASS\|WARN"
    Expected: no output (all tests 14-20 have PASS or WARN, nothing else)

    grep -c "endTest(1[4-9]\|endTest(20" app/Console/Commands/DotwCertify.php
    Expected: >= 7 (at least one endTest per test method 14-20)

    ./vendor/bin/phpstan analyse app/Console/Commands/DotwCertify.php --level=5
    Expected: 0 errors
  </verify>
  <done>
    Tests 14-20 all produce PASS or WARN in storage/logs/dotw_certification.log. Command exits cleanly. No FAIL or ERROR lines for tests 14-20. PHPStan 0 errors.
  </done>
</task>

</tasks>

<verification>
1. php artisan dotw:certify --test=14,15,16,17,18,19,20 → exits cleanly without exceptions
2. storage/logs/dotw_certification.log shows PASS or WARN for each of tests 14-20
3. No test silently missing from log (all 7 have an endTest() verdict)
4. ./vendor/bin/phpstan analyse app/Console/Commands/DotwCertify.php → 0 errors
</verification>

<success_criteria>
- Tests 14-20 all output PASS or WARN (WARN is acceptable for dev environment rate type gaps)
- No PHP fatal, exception, or ERROR verdict for any of tests 14-20
- Every test method has endTest() called on every exit path
</success_criteria>

<output>
After completion, create `.planning/phases/10-dotw-v4-certification/10-02-SUMMARY.md`
</output>
