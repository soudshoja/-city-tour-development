---
phase: 10-dotw-v4-certification
plan: "03"
type: execute
wave: 3
depends_on:
  - "10-01"
  - "10-02"
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/STATE.md
  - .planning/ROADMAP.md
autonomous: false
requirements:
  - CERT-01
  - CERT-02
  - CERT-03
  - CERT-04
  - CERT-05
  - CERT-06
  - CERT-07
  - CERT-08
  - CERT-09
  - CERT-10
  - CERT-11
  - CERT-12
  - CERT-13
  - CERT-14
  - CERT-15
  - CERT-16
  - CERT-17
  - CERT-18
  - CERT-19
  - CERT-20

must_haves:
  truths:
    - "php artisan dotw:certify (no flags) runs all 20 tests and completes cleanly"
    - "storage/logs/dotw_certification.log contains exactly 20 test sections, each with PASS or WARN"
    - "CERT-01 through CERT-20 are marked [x] complete in .planning/REQUIREMENTS.md"
    - "Certification log contains XML request, XML response, and assertions for each test (submittable to DOTW)"
  artifacts:
    - path: "storage/logs/dotw_certification.log"
      provides: "Complete 20-test certification log with XML evidence per test"
      contains: "TEST 20"
    - path: ".planning/REQUIREMENTS.md"
      provides: "All CERT-XX requirements marked complete"
      contains: "[x] **CERT-20**"
  key_links:
    - from: "dotw_certification.log"
      to: "DOTW certification submission"
      via: "20 PASS/WARN verdicts with XML evidence"
      pattern: "TEST (1[0-9]|20).*PASS|WARN"
---

<objective>
Execute the full 20-test certification run, confirm the log is complete and submission-ready, and mark all CERT-XX requirements complete in planning documents.

Purpose: This is the final gate — all 20 tests must have a PASS or WARN verdict in a single complete run. The log file becomes the artifact submitted to DOTW for certification sign-off.

Output: Confirmed-complete certification log at storage/logs/dotw_certification.log, all 20 CERT requirements marked complete.
</objective>

<execution_context>
@/home/soudshoja/.claude/get-shit-done/workflows/execute-plan.md
@/home/soudshoja/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-dotw-v4-certification/10-01-SUMMARY.md
@.planning/phases/10-dotw-v4-certification/10-02-SUMMARY.md
@app/Console/Commands/DotwCertify.php
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full 20-test certification run and log validation</name>
  <files>app/Console/Commands/DotwCertify.php</files>
  <action>
    Run all 20 tests in a single command:

    ```bash
    cd /home/soudshoja/soud-laravel
    php artisan dotw:certify
    ```

    The command should complete without any PHP fatal, ManuallyFailedException, or exception trace. After it finishes, validate the certification log:

    1. Count test sections: `grep -c "^\[TEST" storage/logs/dotw_certification.log` — must be 20
    2. Check all verdicts: `grep -E "^\s*(PASS|FAIL|WARN)" storage/logs/dotw_certification.log` — must show only PASS or WARN (zero FAIL)
    3. Verify XML evidence per test: for each test section, the log must contain at least one XML request block and one XML response block. Check: `grep -c "xml" storage/logs/dotw_certification.log` — must be substantially more than 20 (each test logs multiple XML payloads)
    4. Confirm test 20 appears: `grep "TEST 20" storage/logs/dotw_certification.log` — must exist

    If any test shows FAIL (not WARN) — go back to the relevant test method, inspect the log for that test's XML, diagnose, fix, and re-run `php artisan dotw:certify --test=N` until it passes, then re-run the full suite.

    If the final summary printed by `printSummary()` shows any FAIL, treat as a blocker and resolve before proceeding.

    Once the full run shows all 20 tests as PASS or WARN, copy the final summary block from the console output into the SUMMARY.md created at the end of this plan.
  </action>
  <verify>
    php artisan dotw:certify ; echo "Exit: $?"
    Expected: clean completion, exit code 0

    grep -E "FAIL" storage/logs/dotw_certification.log | grep -v "failStep\|FAIL not found\|no FAIL"
    Expected: no FAIL verdict lines (only PASS and WARN allowed)

    grep -c "TEST [0-9]" storage/logs/dotw_certification.log
    Expected: >= 20

    wc -l storage/logs/dotw_certification.log
    Expected: substantial file (hundreds of lines — XML request + response per test)
  </verify>
  <done>
    All 20 tests produce PASS or WARN in a single complete run. storage/logs/dotw_certification.log is non-empty with 20 test sections and XML evidence. Command exits cleanly.
  </done>
</task>

<task type="auto">
  <name>Task 2: Mark CERT-01..20 complete in planning documents</name>
  <files>.planning/REQUIREMENTS.md</files>
  <action>
    Update .planning/REQUIREMENTS.md:

    In the CERT section, change every `- [ ]` to `- [x]` for CERT-01 through CERT-20. All 20 entries.

    Also update the Traceability table at the bottom — change the Status column from "Pending" to "Complete" for all 20 CERT-XX rows.

    Update the final note at the bottom: change "Last updated: 2026-02-24" to "Last updated: 2026-02-25 — v2.0 DOTW Complete certification tests passed"

    Do NOT change any other requirements (BOOK, CANCEL, LOOKUP, VALID — those are already marked [x]).
  </action>
  <verify>
    grep -c "\- \[x\] \*\*CERT-" .planning/REQUIREMENTS.md
    Expected: 20 (all CERT-XX marked complete)

    grep "\- \[ \] \*\*CERT-" .planning/REQUIREMENTS.md
    Expected: no output (no pending CERT requirements remain)
  </verify>
  <done>
    All 20 CERT-XX requirements marked [x] in REQUIREMENTS.md. Traceability table shows Complete for all CERT rows.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verification — certification log complete and all 20 tests PASS/WARN</name>
  <action>Review certification log and confirm submission readiness</action>
  <verify>
    1. On the server: `php artisan dotw:certify` — confirm clean exit, 20 PASS/WARN summary printed
    2. Check log: `cat storage/logs/dotw_certification.log | grep -E "PASS|WARN|FAIL"` — verify only PASS and WARN
    3. Review a few test sections in the log to confirm XML requests + responses are recorded
    4. Check: `grep -c "\- \[x\] \*\*CERT-" .planning/REQUIREMENTS.md` — must return 20
    5. If any test shows FAIL: note which test number and describe what the log says
  </verify>
  <done>All 20 tests PASS or WARN. Certification log is submission-ready.</done>
  <what-built>
    Full 20-test DOTW V4 certification run against xmldev.dotwconnect.com. All tests produce PASS or WARN. Certification log at storage/logs/dotw_certification.log contains XML evidence per test. All CERT-01..20 requirements marked complete.
  </what-built>
  <how-to-verify>
    1. On the server: `php artisan dotw:certify` — confirm clean exit, 20 PASS/WARN summary printed
    2. Check log: `cat storage/logs/dotw_certification.log | grep -E "PASS|WARN|FAIL"` — verify only PASS and WARN
    3. Review a few test sections in the log to confirm XML requests + responses are recorded
    4. Check: `grep -c "\- \[x\] \*\*CERT-" .planning/REQUIREMENTS.md` — must return 20
    5. If any test shows FAIL: note which test number and describe what the log shows
  </how-to-verify>
  <resume-signal>Type "approved" if all 20 tests are PASS/WARN, or describe which tests failed and what the log shows</resume-signal>
</task>

</tasks>

<verification>
1. php artisan dotw:certify → clean exit, 20/20 summary
2. storage/logs/dotw_certification.log has 20 test sections with PASS/WARN only
3. .planning/REQUIREMENTS.md shows [x] for all CERT-01..20
4. Human verified the log is complete and submission-ready
</verification>

<success_criteria>
- All 20 tests produce PASS or WARN in a single full run of php artisan dotw:certify
- storage/logs/dotw_certification.log contains XML evidence for every test
- CERT-01..20 all marked [x] in REQUIREMENTS.md
- Human has reviewed and approved the certification log
- v2.0 DOTW Complete milestone is achieved
</success_criteria>

<output>
After completion, create `.planning/phases/10-dotw-v4-certification/10-03-SUMMARY.md`
</output>
