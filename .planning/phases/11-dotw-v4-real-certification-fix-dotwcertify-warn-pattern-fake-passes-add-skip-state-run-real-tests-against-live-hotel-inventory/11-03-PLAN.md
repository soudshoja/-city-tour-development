---
phase: 11-dotw-v4-real-certification-fix-dotwcertify-warn-pattern-fake-passes-add-skip-state-run-real-tests-against-live-hotel-inventory
plan: 03
type: execute
wave: 3
depends_on:
  - 11-01
  - 11-02
files_modified:
  - storage/logs/dotw_certification.log
autonomous: false
requirements:
  - REAL-04
  - REAL-05

must_haves:
  truths:
    - "php artisan dotw:certify completes without PHP errors"
    - "The certification log shows at least Test 1 as PASS (real booking flow executed)"
    - "The certification log shows SKIP (not PASS) for tests where no matching hotel inventory was found"
    - "The summary line accurately reflects the honest count: some PASSes, some SKIPs, zero fake PASSes"
    - "If sandbox returns no hotels at all, user is informed what to try (different city code or production credentials)"
  artifacts:
    - path: "storage/logs/dotw_certification.log"
      provides: "Honest certification run result log"
      contains: "CERTIFICATION TEST SUMMARY"
  key_links:
    - from: "php artisan dotw:certify"
      to: "storage/logs/dotw_certification.log"
      via: "DotwCertify::handle() → printSummary()"
      pattern: "RESULT:"
---

<objective>
Execute the corrected DotwCertify against live DOTW API and produce an honest certification log. Attempt to find hotel inventory using the existing `--countries` and `--cities=X` helper flags, then run the full 20-test suite. If sandbox inventory is sparse, document which tests ran vs skipped.

Purpose: Validate that the Phase 11 fixes are working — SKIP not PASS for empty environments, and real PASS for tests with actual inventory. This produces the honest certification state that Phase 10 falsely claimed.

Output: A certification log at `storage/logs/dotw_certification.log` showing real PASS/FAIL/SKIP counts.
</objective>

<execution_context>
@/home/soudshoja/.claude/get-shit-done/workflows/execute-plan.md
@/home/soudshoja/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/11-dotw-v4-real-certification-fix-dotwcertify-warn-pattern-fake-passes-add-skip-state-run-real-tests-against-live-hotel-inventory/11-01-SUMMARY.md
@.planning/phases/11-dotw-v4-real-certification-fix-dotwcertify-warn-pattern-fake-passes-add-skip-state-run-real-tests-against-live-hotel-inventory/11-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Find hotel inventory and run full certification</name>
  <files>storage/logs/dotw_certification.log</files>
  <action>
Run the certification tests in sequence. The goal is to find inventory and produce a real result.

**Step 1 — Inventory discovery (run these first):**
```bash
cd /home/soudshoja/soud-laravel

# Check which countries have hotels in this environment
php artisan dotw:certify --countries 2>&1

# Check cities in UAE (country code 6) — Dubai = 364
php artisan dotw:certify --cities=6 2>&1

# If UAE has no results, try other country codes from --countries output
```

**Step 2 — Single test run first (validate the fix works):**
```bash
php artisan dotw:certify --test=1 2>&1
```

If Test 1 returns hotels and produces PASS, the fix is working. Review the certification log.
If Test 1 returns no hotels, it will SKIP (not PASS as before). This confirms REAL-01 is working.

**Step 3 — Full run:**
```bash
php artisan dotw:certify 2>&1
```

**Step 4 — Review results:**
```bash
cat storage/logs/dotw_certification.log | grep "RESULT:"
cat storage/logs/dotw_certification.log | tail -20
```

**Decision logic:**
- If Test 1 produces PASS: note how many total tests PASS vs SKIP in the summary
- If ALL tests SKIP (no inventory anywhere in sandbox): this is acceptable — the SKIP state confirms REAL-01 is satisfied. Document this in the SUMMARY.md.
- If any test produces FAIL with a specific error: investigate whether it is a confirmbooking XML error (REAL-03 regression) or a DOTW API error. Fix if needed.

**Do NOT switch to production credentials** unless explicitly instructed by the user. Stay with `dev_mode=true` (sandbox). The goal of this plan is to verify the SKIP fix works and document the honest state, not necessarily to achieve 20/20 PASS.
  </action>
  <verify>
    <automated>cd /home/soudshoja/soud-laravel && cat storage/logs/dotw_certification.log | grep -E "RESULT:|SUMMARY|Total:" | head -30</automated>
  </verify>
  <done>
    - `php artisan dotw:certify` completes without PHP fatal errors
    - `storage/logs/dotw_certification.log` exists and contains "CERTIFICATION TEST SUMMARY" section
    - The summary shows "Passed: X | Failed: X | Skipped: X | Not Run: X" (four distinct counts)
    - If any hotels were found: at least one test shows "RESULT: PASS" in the log
    - No test that had no hotel inventory shows "RESULT: PASS" (they show "RESULT: SKIP")
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verify certification log is honest</name>
  <files>storage/logs/dotw_certification.log</files>
  <action>
CHECKPOINT — Pause for human verification. Claude pauses here and waits for user review of the certification log.

Steps for human to perform:
1. `cat /home/soudshoja/soud-laravel/storage/logs/dotw_certification.log | grep "RESULT:"`
2. Confirm NO line shows "RESULT: PASS" for a test where no hotels were found
3. Confirm lines with no hotels show "RESULT: SKIP"
4. `cat /home/soudshoja/soud-laravel/storage/logs/dotw_certification.log | tail -15`
5. Summary should show four distinct counts (Passed / Failed / Skipped / Not Run) — not "Passed: 20"
6. If Test 1 ran with real hotels and shows PASS, verify confirmbooking XML in the log has correct element order
  </action>
  <verify>
    <automated>cat /home/soudshoja/soud-laravel/storage/logs/dotw_certification.log | tail -10</automated>
  </verify>
  <done>User types "approved" to confirm the certification log is honest (SKIP not PASS for empty tests).</done>
</task>

</tasks>

<verification>
After Task 1 and human verification:

1. `cat storage/logs/dotw_certification.log | grep "RESULT: PASS" | wc -l` — count of real PASSes (could be 0 if sandbox has no inventory; that is acceptable)
2. `cat storage/logs/dotw_certification.log | grep "RESULT: SKIP" | wc -l` — count of honest SKIPs (should be greater than 0 if sandbox has no inventory)
3. `cat storage/logs/dotw_certification.log | grep "Passed: 20"` — should return NOTHING (20/20 PASS is no longer acceptable as a result)
4. Manually verify REAL-04: if any test produced PASS, confirm it executed a real API flow (check for request/response XML in the log)
</verification>

<success_criteria>
- `php artisan dotw:certify` runs without PHP errors
- Certification log contains honest PASS/SKIP/FAIL/NOT RUN counts
- No test with zero hotel inventory is recorded as PASS
- Phase 11 REAL-01 through REAL-05 all satisfied (or REAL-04 noted as "sandbox has no inventory — requires production run")
- User approves the honest certification state
</success_criteria>

<output>
After completion, create `.planning/phases/11-dotw-v4-real-certification-fix-dotwcertify-warn-pattern-fake-passes-add-skip-state-run-real-tests-against-live-hotel-inventory/11-03-SUMMARY.md`
</output>
