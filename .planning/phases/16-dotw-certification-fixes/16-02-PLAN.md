---
phase: 16-dotw-certification-fixes
plan: 02
type: execute
wave: 2
depends_on: ["16-01"]
files_modified:
  - app/Console/Commands/DotwCertify.php
autonomous: true
requirements:
  - DOTW-FIX-05
  - DOTW-FIX-06

must_haves:
  truths:
    - "Test 14 (changedOccupancy) sends adultsCode from validForOccupancy but actualAdults/actualChildren from original search"
    - "Test 15 (Special Promotions) uses hotel 2344175 with check-in 14 May 2026, 2A+2C ages 8,12 and finds specials"
    - "Test 6 (cancel within deadline) no longer SKIPs due to sandbox limitations"
    - "Tests 16, 17, 18, 20 use targeted hotel searches to find required rate features"
    - "All 20 tests produce PASS (zero SKIP, zero FAIL)"
  artifacts:
    - path: "app/Console/Commands/DotwCertify.php"
      provides: "Certification suite with changedOccupancy fix and all SKIP tests converted to PASS"
  key_links:
    - from: "app/Console/Commands/DotwCertify.php runTest14"
      to: "DOTW validForOccupancy response"
      via: "adultsCode from validForOccupancy->adults, actualAdults=3 from original search"
      pattern: "adultsCode.*validForOccupancy|actualAdults.*3"
    - from: "app/Console/Commands/DotwCertify.php runTest15"
      to: "DOTW hotel 2344175"
      via: "Direct hotelid search for The S Hotel Al Barsha"
      pattern: "2344175"
---

<objective>
Fix changedOccupancy implementation (issue 5) and convert all SKIP tests to PASS using DOTW-provided hotel IDs (issue 6).

Purpose: DOTW certification requires ALL 20 tests to PASS. Currently 5 tests SKIP due to sandbox data limitations. DOTW has provided specific hotel IDs that have the required features. The changedOccupancy fix ensures correct XML structure per DOTW's exact specification.
Output: Updated DotwCertify.php where all 20 tests PASS.
</objective>

<execution_context>
@C:/Users/User/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/User/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-dotw-certification-fixes/16-CONTEXT.md
@.planning/phases/16-dotw-certification-fixes/16-01-SUMMARY.md

<interfaces>
<!-- From 16-CONTEXT.md - DOTW-provided hotel hints -->
Special Promotions (test 15): Hotel ID 2344175 (The S Hotel Al Barsha), Dubai, check-in 14 May 2026, check-out 15 May 2026, 2 adults + 2 children (ages 8 and 12)
MSP (test 11): Hotel ID 809755 (Conrad London St James)

<!-- changedOccupancy CORRECT XML structure per DOTW feedback -->
<adultsCode>{from validForOccupancy->adults}</adultsCode>
<actualAdults>{from original search, e.g. 3}</actualAdults>
<children no="{from validForOccupancy->children count}">
  {validForOccupancy children}
</children>
<actualChildren no="{from original search child count}">
  <actualChild runno="0">{original search child age}</actualChild>
</actualChildren>
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix changedOccupancy XML structure in test 14</name>
  <files>app/Console/Commands/DotwCertify.php</files>
  <read_first>app/Console/Commands/DotwCertify.php (lines 2216-2470, the entire runTest14 method)</read_first>
  <action>
The changedOccupancy test (test 14) currently has a subtle issue in the confirmbooking XML. Per DOTW feedback, the correct structure must be:

- `<adultsCode>` = from `validForOccupancy->adults` (e.g., 4 if DOTW converts child to adult)
- `<actualAdults>` = from ORIGINAL search (3, the real number of adults)
- `<children>` = from `validForOccupancy->children` (e.g., `no="0"` if DOTW absorbed the child)
- `<actualChildren>` = from ORIGINAL search children (e.g., `no="1"` with child age 12)

Review the current code at lines 2394-2416. The current implementation at line 2411-2416:
```xml
<adultsCode>{$bookAdultsCode}</adultsCode>  <!-- This is from validForOccupancy - CORRECT -->
<actualAdults>3</actualAdults>  <!-- This is from original search - CORRECT -->
{$bookChildrenXml}  <!-- This is from validForOccupancy children - CORRECT -->
<actualChildren no="1">  <!-- This is from original search - CORRECT -->
    <actualChild runno="0">12</actualChild>
</actualChildren>
```

The code looks correct but verify the `$bookChildrenXml` variable construction (lines 2324-2338). When `validForOccupancy->children` has `no="0"`, the `$bookChildrenXml` should produce `<children no="0"></children>` or `<children no="0"/>`.

**Key change needed:** The `$bookChildrenXml` default at line 2314 is:
```php
$bookChildrenXml = '<children no="1"><child runno="0">12</child></children>';
```
This default is the ORIGINAL search children and should NOT be used as the `<children>` node in confirmbooking when changedOccupancy is detected. The `<children>` node must come from validForOccupancy.

Verify the if/else flow:
1. If `$hasChangedOcc` is true (line 2317), the code at lines 2326-2338 builds `$bookChildrenXml` from `validForOccupancy->children` - this is CORRECT
2. The `$bookChildrenXml` default at line 2314 is only used if changedOccupancy is NOT detected (but in that case the test SKIPs at line 2340)

So the logic flow IS correct, but add explicit logging to make the XML structure visible for DOTW certification review:
```php
$this->log("  VERIFICATION XML: <adultsCode>{$bookAdultsCode}</adultsCode> (from validForOccupancy)");
$this->log("  VERIFICATION XML: <actualAdults>3</actualAdults> (from original search)");
$this->log("  VERIFICATION XML: {$bookChildrenXml} (from validForOccupancy)");
$this->log("  VERIFICATION XML: <actualChildren no=\"1\"><actualChild runno=\"0\">12</actualChild></actualChildren> (from original search)");
```

Add this logging BEFORE the confirmbooking XML is built (after line 2396, before line 2397).

Also add a comment block above the confirmbooking XML construction explaining the dual-source pattern:
```php
// DOTW changedOccupancy dual-source pattern:
// adultsCode + children  = from validForOccupancy (pricing occupancy)
// actualAdults + actualChildren = from original search (real occupancy)
```
  </action>
  <verify>
    <automated>cd "C:\Users\User\OneDrive - City Travelers\soud-laravel" && grep -n "VERIFICATION XML.*adultsCode.*validForOccupancy\|VERIFICATION XML.*actualAdults.*original\|dual-source pattern" app/Console/Commands/DotwCertify.php | head -10 && ./vendor/bin/phpstan analyse app/Console/Commands/DotwCertify.php --no-progress 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
- `grep -c "VERIFICATION XML" app/Console/Commands/DotwCertify.php` returns at least 4 (the 4 logging lines)
- `grep -c "dual-source pattern" app/Console/Commands/DotwCertify.php` returns at least 1
- The confirmbooking XML in test 14 uses `$bookAdultsCode` (from validForOccupancy) for adultsCode
- The confirmbooking XML in test 14 uses hardcoded `3` for actualAdults (from original search)
- The confirmbooking XML in test 14 uses `$bookChildrenXml` (from validForOccupancy) for children
- The confirmbooking XML in test 14 uses hardcoded `<actualChildren no="1"><actualChild runno="0">12</actualChild></actualChildren>` (from original search)
- PHPStan passes
  </acceptance_criteria>
  <done>changedOccupancy confirmbooking XML explicitly documented with dual-source pattern. Verification logging added for DOTW certification review. PHPStan passes.</done>
</task>

<task type="auto">
  <name>Task 2: Convert all SKIP tests to PASS using DOTW-provided hotel IDs and targeted searches</name>
  <files>app/Console/Commands/DotwCertify.php</files>
  <read_first>app/Console/Commands/DotwCertify.php (focus on: runTest6 lines 1754-2075, runTest15 lines 2475-2593, runTest16 lines 2598-2875, runTest17 lines 2878-3120, runTest18 lines 3125-3247, runTest20 lines 3446-3524)</read_first>
  <action>
Convert all 6 SKIP tests to PASS. The previously-SKIP tests are: 6, 15, 16, 17, 18, 20.

**Test 15 — Special Promotions (DOTW provided hotel 2344175):**

Rewrite `runTest15()` to use DOTW-provided parameters:
- Hotel ID: 2344175 (The S Hotel Al Barsha)
- City: Dubai (364)
- Check-in: 2026-05-14
- Check-out: 2026-05-15
- Occupancy: 2 adults + 2 children (ages 8 and 12)

Instead of searching broadly and hoping to find specials, search directly by hotel ID using the `hotelIdsToSearch` filter approach. The searchhotels can filter by hotelid:
```xml
<bookingDetails>
    <fromDate>2026-05-14</fromDate>
    <toDate>2026-05-15</toDate>
    <currency>769</currency>
    <rooms no="1">
        <room runno="0">
            <adultsCode>2</adultsCode>
            <children no="2">
                <child runno="0">8</child>
                <child runno="1">12</child>
            </children>
            <rateBasis>-1</rateBasis>
            <passengerNationality>66</passengerNationality>
            <passengerCountryOfResidence>66</passengerCountryOfResidence>
        </room>
    </rooms>
</bookingDetails>
<return>
    <filters xmlns:a="http://us.dotwconnect.com/xsd/atomicCondition"
             xmlns:c="http://us.dotwconnect.com/xsd/complexCondition">
        <city>364</city>
        <c:condition>
            <a:condition>
                <fieldName>hotelid</fieldName>
                <fieldTest>in</fieldTest>
                <fieldValues>
                    <fieldValue>2344175</fieldValue>
                </fieldValues>
            </a:condition>
        </c:condition>
    </filters>
</return>
```

Then proceed with normal browse (requesting `specials` roomField) + check for specialsApplied. If the hotel has active promotions (DOTW confirmed it does), specials will be found and the test PASSes.

Keep the existing fallback logic: if specials are NOT found on this specific hotel, still SKIP with a clear message (but this should not happen with DOTW's recommended hotel).

**Test 11 — MSP (DOTW provided hotel 809755):**

Test 11 currently PASSES. DOTW mentioned hotel 809755 (Conrad London St James) as an MSP reference. No changes needed if test 11 already passes. But if the current test 11 does a broad search that might become flaky, consider adding hotel 809755 as a targeted search similar to test 15.

**Test 6 — Cancel within deadline:**

Test 6 currently SKIPs because sandbox returns error 60 (deadline expired) on the cancellation. The issue is that with near-future dates (2 days out), the cancellation deadline has already passed.

Fix approach: Use dates further in the future (e.g., 60+ days out) so the cancellation deadline hasn't passed yet. Change:
```php
$fromDate = now()->addDays(2)->format('Y-m-d');
$toDate = now()->addDays(3)->format('Y-m-d');
```
to:
```php
$fromDate = now()->addDays(60)->format('Y-m-d');
$toDate = now()->addDays(61)->format('Y-m-d');
```

Also change room 0 `rateBasis` from 1331 to -1 (if not already done in plan 01 — verify). Ensure the cancel step handles the response properly. The cancel should work if the booking is far enough in the future that no penalty applies, but we WANT a penalty (this is "cancel within deadline").

Actually, re-read test 6 purpose: "Cancel 2-room booking within cancellation deadline — cancel with penalty". The SKIPs were from error 60 = "deadline expired" meaning the deadline for free cancellation has passed, so the cancel has a charge. This actually SHOULD work. The error 60 may be a different sandbox issue.

Alternative fix for test 6: The error 60 might happen because dates are too close. Use dates 30-60 days out and use `requireCancellable = false` to allow restricted rates. If test 6 is about cancel-WITH-penalty, we specifically want a rate where cancellation within deadline incurs a charge. Try using `tryBookHotels()` with dates far enough out to get a valid booking, then cancel. Check if the cancellation response includes `charges` or `penaltyAmount`.

If test 6 still fails due to sandbox limitations after date adjustment, implement a try-multiple-hotels approach similar to what tests 1-5 already do.

**Test 16 — APR Booking:**

Currently SKIPs because no nonrefundable=yes rates found. With pagination removed (plan 01), the search may return different/more results. If still no APR rates:
- Try searching London (city code for London — look it up using DOTW reference data or try common codes: 100 for London area)
- Try searching with hotel 809755 (Conrad London St James) which DOTW provided — APR rates may be available there
- Try alternative cities: Bangkok, Singapore, etc.

Add a multi-city search approach: Dubai first, then London, then a third city.

**Test 17 — Restricted Cancellation:**

Currently SKIPs after scanning 6 hotels. Fix:
- Increase scan depth: scan ALL returned hotels (not just first 3 per page)
- Try additional cities beyond Dubai
- Use the DOTW-provided hotel 809755 (Conrad London) — luxury hotels often have restricted cancellation policies

**Test 18 — Minimum Stay:**

Currently SKIPs after scanning 5 hotels. Fix:
- Scan ALL returned hotels (not just 5)
- Try longer stays (3-4 nights) which are more likely to trigger minStay constraints
- Try alternative cities / resort destinations where minStay is common

**Test 20 — Property Taxes/Fees:**

Currently SKIPs — no propertyFees found. Fix:
- Try London hotels (UK has VAT-related property fees)
- Try US city hotels (resort fees are common)
- Scan ALL returned hotels (not just the first batch)

**General approach for tests 16-20:** Each of these tests searches for a specific rate feature. The SKIPs happen because the sandbox doesn't have these features for Dubai hotels. Strategy: try multiple cities and scan more hotels. If DOTW-provided hotel IDs work, use those as primary targets.

IMPORTANT: After making all changes, ensure `./vendor/bin/phpstan analyse app/Console/Commands/DotwCertify.php` passes. Also run `./vendor/bin/pint app/Console/Commands/DotwCertify.php` for code formatting.
  </action>
  <verify>
    <automated>cd "C:\Users\User\OneDrive - City Travelers\soud-laravel" && grep -n "2344175" app/Console/Commands/DotwCertify.php | head -5 && echo "---" && grep -c "skipTest" app/Console/Commands/DotwCertify.php && echo "---" && ./vendor/bin/phpstan analyse app/Console/Commands/DotwCertify.php --no-progress 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
- `grep -c "2344175" app/Console/Commands/DotwCertify.php` returns at least 1 (DOTW hotel for test 15)
- Test 15 uses hardcoded dates 2026-05-14 to 2026-05-15 with 2A+2C (8,12)
- Test 6 uses dates 60+ days out instead of 2 days
- Tests 16, 17, 18, 20 have expanded multi-city search strategies
- `./vendor/bin/phpstan analyse app/Console/Commands/DotwCertify.php` passes
- The `skipTest` calls should be reduced compared to before (some may remain as absolute last-resort fallbacks, but the primary paths should PASS)
  </acceptance_criteria>
  <done>All 6 previously-SKIP tests updated with DOTW-provided hotel IDs and expanded search strategies. Test 15 uses hotel 2344175 with exact DOTW-specified parameters. Tests have fallback SKIP only as absolute last resort. PHPStan passes.</done>
</task>

</tasks>

<verification>
Run the certification suite to verify all tests pass:
```bash
# Quick syntax check
./vendor/bin/phpstan analyse app/Console/Commands/DotwCertify.php

# Format check
./vendor/bin/pint --test app/Console/Commands/DotwCertify.php

# Full certification run (takes ~5-10 minutes against sandbox)
php artisan dotw:certify 2>&1 | tail -30

# Check for remaining SKIPs
grep -c "SKIP" storage/logs/dotw_certification.log
```
</verification>

<success_criteria>
1. changedOccupancy test 14 has explicit dual-source verification logging
2. Test 15 uses DOTW hotel 2344175 with exact specified parameters
3. Test 6 uses far-future dates to avoid deadline expiry
4. Tests 16, 17, 18, 20 have multi-city expanded search strategies
5. PHPStan and Pint pass
6. When run against sandbox: target 19-20 PASS / 0-1 SKIP / 0 FAIL
</success_criteria>

<output>
After completion, create `.planning/phases/16-dotw-certification-fixes/16-02-SUMMARY.md`
</output>
