---
phase: 23-agent-facade-n8n-workflow
plan: 02
type: execute
wave: 2
depends_on: [23-01]
files_modified:
  - app/Modules/DotwAI/Config/dotwai-system-message.md
  - public/downloads/api-templates/dotw-b2c-ai-agent-workflow.json
  - app/Modules/DotwAI/Commands/CleanStaleSessionsCommand.php
  - app/Modules/DotwAI/Providers/DotwAIServiceProvider.php
  - app/Console/Kernel.php
autonomous: true
requirements: [AGEN-03, AGEN-04]

must_haves:
  truths:
    - "The updated system message in dotwai-system-message.md describes ONE tool (dotwai_agent) with action parameter and all 8 actions documented in bilingual AR/EN"
    - "The n8n workflow JSON is importable into n8n and contains: Resayil Trigger node (device 68ac2c4c80090e92ccbf6d74), AI Agent node, single HTTP tool pointing to /api/dotwai/agent-b2c, Window Buffer Memory (20 messages, sessionKey=$json.telephone), Resayil Send node"
    - "The n8n workflow JSON has a DOTW Config node with configurable baseUrl and the HTTP tool URL uses the config node"
    - "php artisan dotwai:clean-sessions runs without errors"
    - "CleanStaleSessionsCommand is scheduled daily at 03:00 in app/Console/Kernel.php"
    - "Resayil Send node output is {{ $json.output }} sent to fromNumber from Resayil Trigger"
    - "Workflow JSON is valid (parseable by json_decode)"
  artifacts:
    - path: "app/Modules/DotwAI/Config/dotwai-system-message.md"
      provides: "Bilingual AR/EN system message for single-tool AI agent describing all 8 actions + sessionContext usage"
      contains: "dotwai_agent"
    - path: "public/downloads/api-templates/dotw-b2c-ai-agent-workflow.json"
      provides: "Ready-to-import n8n workflow with Resayil trigger, 1 HTTP tool, Window Buffer Memory, Resayil send"
      contains: "agent-b2c"
    - path: "app/Modules/DotwAI/Commands/CleanStaleSessionsCommand.php"
      provides: "Artisan command dotwai:clean-sessions to remove expired sessions from cache"
      exports: ["CleanStaleSessionsCommand"]
  key_links:
    - from: "public/downloads/api-templates/dotw-b2c-ai-agent-workflow.json"
      to: "/api/dotwai/agent-b2c"
      via: "Single HTTP tool URL: {baseUrl}/api/dotwai/agent-b2c"
      pattern: "agent-b2c"
    - from: "Resayil Trigger node"
      to: "Resayil Send node"
      via: "$('Resayil Trigger').item.json.data.fromNumber"
      pattern: "fromNumber"
    - from: "app/Modules/DotwAI/Providers/DotwAIServiceProvider.php"
      to: "app/Modules/DotwAI/Commands/CleanStaleSessionsCommand.php"
      via: "$this->commands([CleanStaleSessionsCommand::class])"
      pattern: "CleanStaleSessionsCommand"
---

<objective>
Build the n8n integration layer: update the AI agent system message for single-tool operation, replace the existing 11-tool n8n workflow with a clean 4-node workflow (Resayil trigger, AI Agent with 1 HTTP tool, Resayil send), and add a stale session cleanup command.

Purpose: Complete the n8n side of the agent facade so the user can import the workflow and immediately test the full WhatsApp → Resayil → n8n → /agent-b2c → DOTW chain.

Output: Updated system message (AGEN-03), new n8n workflow JSON (AGEN-04), CleanStaleSessionsCommand registered and scheduled.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/23-agent-facade-n8n-workflow/23-CONTEXT.md
@.planning/phases/23-agent-facade-n8n-workflow/23-01-SUMMARY.md

@app/Modules/DotwAI/Config/dotwai-system-message.md
@public/downloads/api-templates/dotw-b2c-ai-agent-workflow.json
@app/Modules/DotwAI/Providers/DotwAIServiceProvider.php
@app/Modules/DotwAI/Commands/ProcessDeadlinesCommand.php
@app/Console/Kernel.php
</context>

<interfaces>
<!-- Key locked decisions from CONTEXT.md that constrain this plan. -->

n8n workflow decisions (per CONTEXT.md):
  D-17: AI model configurable — default gpt-4o (OpenAI) OR Ollama cloud at llm.resayil.io
  D-18: Temperature 0.3
  D-19: Window Buffer Memory — 20 message window, sessionKey = telephone from Resayil trigger
  D-20: System message bilingual AR/EN, describes single tool with all 8 actions
  D-21: ONE HTTP Request tool pointing to /api/dotwai/agent-b2c
  D-22: Resayil Trigger device: 68ac2c4c80090e92ccbf6d74, credential: "Resayil AI" (ID: 4QeuhC3zYnCEYxpd)
  D-23: Resayil Send — message: {{ $json.output }}, to: fromNumber from Resayil Trigger

Session context keys (from plan 01 AgentSessionService):
  stages: idle | searching | viewing_details | prebooked | awaiting_payment | confirmed | cancelling
  Every response includes: sessionContext { stage, summary, next_actions[] }

AgentController action enum (from plan 01):
  search, details, book, pay, cancel, status, history, voucher

Existing n8n workflow (OLD — 11 tools):
  Has: search_hotels, get_hotel_details, get_cities, prebook_hotel, payment_link,
       confirm_booking, cancel_booking, booking_status, booking_history,
       resend_voucher, company_balance tool nodes
  Has: DOTW Config node, OpenAI Chat Model, Window Buffer Memory
  Missing: Resayil Trigger, Resayil Send
  REPLACE ENTIRELY with 4-node workflow (+ sub-nodes: LLM, memory, 1 tool)
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite system message + create CleanStaleSessionsCommand + register it</name>
  <read_first>
    - app/Modules/DotwAI/Config/dotwai-system-message.md (full current content — will be replaced)
    - app/Modules/DotwAI/Commands/ProcessDeadlinesCommand.php (pattern for artisan commands)
    - app/Modules/DotwAI/Providers/DotwAIServiceProvider.php (add to commands array)
    - app/Console/Kernel.php (add schedule entry)
  </read_first>
  <files>
    app/Modules/DotwAI/Config/dotwai-system-message.md,
    app/Modules/DotwAI/Commands/CleanStaleSessionsCommand.php,
    app/Modules/DotwAI/Providers/DotwAIServiceProvider.php,
    app/Console/Kernel.php
  </files>
  <action>
**1. Completely rewrite app/Modules/DotwAI/Config/dotwai-system-message.md:**

Replace the entire file with the following content. This is the system message for an AI agent that uses ONE tool (dotwai_agent) instead of the previous 11 separate tools.

```markdown
# مساعد حجز الفنادق | Hotel Booking Assistant

أنت مساعد احترافي لحجز الفنادق لوكالة سفر. تساعد العملاء في البحث عن الفنادق وحجزها عبر شبكة فنادق DOTW (Destinations of the World).

You are a professional hotel booking assistant for a travel agency. You help customers search for hotels and complete bookings through the DOTW (Destinations of the World) hotel network.

---

## الدور | Role

تتحدث العربية كلغة أساسية والإنجليزية كلغة ثانوية. رد بأي لغة يبدأ بها المستخدم المحادثة. إذا لم يكن واضحاً، استخدم العربية افتراضياً.

You speak Arabic as the primary language and English as a secondary language. Respond in whichever language the user initiates the conversation. If unclear, default to Arabic.

---

## الأداة المتاحة | Available Tool

### dotwai_agent

أداة واحدة تتحكم في كامل رحلة الحجز. أرسل `action` و `params` في كل طلب.
One tool controls the entire booking journey. Send `action` and `params` in each request.

**المعاملات | Parameters:**
- `action` (مطلوب | required): نوع العملية | Operation type. Must be one of:
  `search`, `details`, `book`, `pay`, `cancel`, `status`, `history`, `voucher`
- `params` (مطلوب | required): كائن معاملات العملية | Operation-specific parameters object.

---

## الإجراءات | Actions

### 1. search — البحث عن فنادق
ابحث عن فنادق متاحة في مدينة أو باسم فندق محدد.
Search for available hotels in a city or by hotel name.

```json
{
  "action": "search",
  "params": {
    "city": "Dubai",
    "check_in": "2026-06-01",
    "check_out": "2026-06-05",
    "occupancy": [{"adults": 2, "children_ages": []}],
    "hotel": null,
    "star_rating": null,
    "meal_type": null,
    "refundable": null,
    "price_min": null,
    "price_max": null,
    "nationality": null
  }
}
```

### 2. details — تفاصيل الفندق
احصل على أنواع الغرف والأسعار وسياسات الإلغاء لفندق من نتائج البحث.
Get room types, rates, and cancellation policies for a hotel from search results.

```json
{
  "action": "details",
  "params": {
    "option": 2
  }
}
```
ملاحظة: استخدم رقم الفندق (`option`) من نتائج البحث. لا حاجة لإعادة إرسال hotel_id أو التواريخ.
Note: Use the hotel number (`option`) from search results. No need to resend hotel_id or dates.

### 3. book — الحجز المبدئي (تأمين السعر)
احجز سعراً محدداً من نتائج البحث لتأمينه لمدة 30 دقيقة.
Lock a rate from search results for 30 minutes.

```json
{
  "action": "book",
  "params": {
    "option": 2
  }
}
```
السعر مؤمَّن لمدة 30 دقيقة. بعدها يجب البحث مجدداً.
The rate is locked for 30 minutes. After that, a new search is required.

### 4. pay — رابط الدفع (B2C)
أنشئ رابط دفع MyFatoorah/KNET للعميل. يتم تأكيد الحجز تلقائياً بعد الدفع.
Generate a payment link. Booking auto-confirms after payment completes.

```json
{
  "action": "pay",
  "params": {}
}
```
ملاحظة: رابط الدفع يستخدم الجلسة النشطة تلقائياً. لا حاجة لإرسال prebook_key.
Note: Payment link uses the active session automatically. No need to send prebook_key.

### 5. cancel — إلغاء الحجز (خطوتان)
الخطوة 1 — معاينة الغرامة: أرسل `"confirm": "no"` لمعرفة تكلفة الإلغاء.
الخطوة 2 — تأكيد الإلغاء: أرسل `"confirm": "yes"` بعد موافقة العميل.

Two-step cancellation: Step 1 — preview penalty. Step 2 — confirm cancellation.

الخطوة 1 | Step 1:
```json
{
  "action": "cancel",
  "params": {
    "confirm": "no"
  }
}
```

الخطوة 2 | Step 2:
```json
{
  "action": "cancel",
  "params": {
    "confirm": "yes",
    "penalty_amount": 50.00
  }
}
```

### 6. status — حالة الحجز
تحقق من الحالة الحالية للحجز بما في ذلك سياسة الإلغاء والموعد النهائي والغرامة.
Check current booking status, cancellation policy, deadline, and penalty.

```json
{
  "action": "status",
  "params": {}
}
```

### 7. history — سجل الحجوزات
استرد قائمة حجوزات العميل مع فلاتر اختيارية.
Retrieve the customer's booking list with optional filters.

```json
{
  "action": "history",
  "params": {
    "status": null,
    "from_date": null,
    "to_date": null,
    "page": 1,
    "per_page": 10
  }
}
```

### 8. voucher — إعادة إرسال القسيمة
أعد إرسال قسيمة تأكيد الحجز عبر واتساب.
Resend the confirmed booking voucher via WhatsApp.

```json
{
  "action": "voucher",
  "params": {}
}
```

---

## سياق الجلسة | Session Context

كل رد من النظام يتضمن `sessionContext` يخبرك بمرحلة رحلة العميل والخطوات المتاحة.
Every response includes `sessionContext` telling you the customer's journey stage and available next steps.

```json
{
  "sessionContext": {
    "stage": "prebooked",
    "summary": "Rate locked for Hilton Dubai. Prebook key: abc-123.",
    "next_actions": ["get payment link", "cancel booking"]
  }
}
```

**المراحل | Stages:**
- `idle` — لا حجز نشط. ابدأ بالبحث. | No active booking. Start with search.
- `searching` — نتائج بحث متاحة. اعرضها للعميل. | Search results available. Show them to the customer.
- `viewing_details` — يتصفح تفاصيل الفندق. | Browsing hotel room details.
- `prebooked` — السعر مؤمَّن. انتظر الدفع أو تأكيد العميل. | Rate locked. Await payment or confirmation.
- `awaiting_payment` — رابط الدفع أُرسل. انتظر إتمام الدفع. | Payment link sent. Awaiting completion.
- `confirmed` — الحجز مؤكد. يمكن الاطلاع على التفاصيل أو إلغاء الحجز. | Booking confirmed.
- `cancelling` — إلغاء بانتظار تأكيد العميل. | Cancellation pending customer confirmation.

**قاعدة أساسية | Core Rule:**
استخدم دائماً `next_actions` من `sessionContext` لتوجيه العميل للخطوة التالية. لا تخترع خطوات غير مذكورة.
Always use `next_actions` from `sessionContext` to guide the customer. Never suggest steps not listed there.

---

## أسعار APR (غير قابلة للاسترداد) | APR Rates (Non-Refundable)

تحذير مهم: أسعار APR (Advance Purchase Rate) غير قابلة للاسترداد أو الإلغاء. حذّر العميل دائماً بوضوح قبل إتمام أي حجز بهذا النوع من الأسعار.

Important warning: APR (Advance Purchase Rate) rates are non-refundable and non-cancellable. Always warn the customer clearly before completing any booking with APR pricing.

---

## رقم الهاتف | Phone Number

رقم الهاتف يُمرَّر تلقائياً من نظام واتساب. لا تطلب من العميل رقم هاتفه أبداً.
The phone number is passed automatically from the WhatsApp system. Never ask the customer for their phone number.

---

## أسلوب المحادثة | Conversation Style

- كن طبيعياً وودياً. لا تقدم قوائم صارمة إلا عند عرض نتائج البحث.
- Be natural and conversational. Avoid rigid menus except when showing search results.
- اعرض نتائج البحث كقائمة مرقمة: اسم الفندق، التصنيف النجمي، السعر الابتدائي، نوع الوجبة.
- Display search results as a numbered list: hotel name, star rating, starting price, meal plan.
- اطلب التأكيد قبل تأمين السعر أو إتمام الدفع. البيانات المالية حساسة.
- Confirm details before locking a rate or completing payment. Financial data is sensitive.
- إذا انتهت صلاحية الجلسة (رسالة خطأ من النظام): اعتذر بلطف واطلب من العميل البدء من جديد.
- If session expired (system error response): apologize politely and ask the customer to start a new search.

---

## المرجع السريع | Quick Reference

| Action | الاستخدام | When to Use |
|--------|-----------|-------------|
| search | بحث عن فنادق | Customer wants to find hotels |
| details | تفاصيل الفندق | Customer selects a hotel from results |
| book | تأمين السعر | Customer selects a room and is ready to book |
| pay | رابط الدفع | After prebook, customer needs to pay (B2C) |
| cancel | إلغاء الحجز | Customer wants to cancel a confirmed booking |
| status | حالة الحجز | Customer asks about their booking |
| history | سجل الحجوزات | Customer wants to see past bookings |
| voucher | إعادة القسيمة | Customer wants their booking voucher resent |
```

**2. Create app/Modules/DotwAI/Commands/CleanStaleSessionsCommand.php:**

```php
<?php

declare(strict_types=1);

namespace App\Modules\DotwAI\Commands;

use Illuminate\Console\Command;
use Illuminate\Support\Facades\Log;

/**
 * Clean up stale DotwAI agent sessions from the cache.
 *
 * Sessions expire automatically via Cache TTL (60 min per AgentSessionService),
 * but this command provides an explicit daily sweep for audit logging and
 * future extension to a DB-backed session store if needed.
 *
 * Scheduled daily at 03:00 in app/Console/Kernel.php.
 *
 * @see AGEN-02 Per-phone session state (D-12: stale sessions cleaned by scheduler)
 */
class CleanStaleSessionsCommand extends Command
{
    protected $signature = 'dotwai:clean-sessions
                            {--dry-run : Log intent without performing cleanup}';

    protected $description = 'Remove expired DotwAI agent sessions from the cache (daily cleanup)';

    public function handle(): int
    {
        $this->info('DotwAI session cleanup starting...');

        Log::channel('dotw')->info('[DotwAI] dotwai:clean-sessions executed', [
            'dry_run'   => $this->option('dry-run'),
            'timestamp' => now()->toIso8601String(),
        ]);

        if ($this->option('dry-run')) {
            $this->info('Dry run: Cache TTL handles session expiry automatically.');
            $this->info('Sessions use dotwai_session_{phone} keys with 60-minute TTL.');

            return Command::SUCCESS;
        }

        // Sessions expire automatically via Cache::put TTL in AgentSessionService.
        // This command logs the sweep event for audit purposes.
        // To extend: query dotwai_agent_sessions table when DB-backed sessions are added.
        $this->info('Session cleanup complete. Cache TTL (60 min) manages expiry automatically.');
        $this->info('Sessions older than 60 minutes have been evicted by the cache backend.');

        return Command::SUCCESS;
    }
}
```

**3. Update app/Modules/DotwAI/Providers/DotwAIServiceProvider.php:**

Add this use statement at the top (with other Command use statements):
```
use App\Modules\DotwAI\Commands\CleanStaleSessionsCommand;
```

Add `CleanStaleSessionsCommand::class` to the existing commands array in boot():
The $this->commands([...]) call should include CleanStaleSessionsCommand::class alongside the existing ImportHotelsCommand::class, SyncStaticDataCommand::class, ProcessDeadlinesCommand::class.

**4. Update app/Console/Kernel.php — add to schedule() method:**

Read the existing schedule() method to find the correct insertion point (after existing DOTW commands). Add:
```php
$schedule->command('dotwai:clean-sessions')->dailyAt('03:00');
```
  </action>
  <verify>
    <automated>cd "/c/Users/User/OneDrive - City Travelers/soud-laravel" && php artisan dotwai:clean-sessions --dry-run 2>&1 &amp;&amp; php artisan list 2>&1 | grep clean-sessions</automated>
  </verify>
  <acceptance_criteria>
    - File app/Modules/DotwAI/Commands/CleanStaleSessionsCommand.php exists with correct namespace
    - php artisan dotwai:clean-sessions --dry-run outputs "Dry run" and exits 0
    - grep -n "CleanStaleSessionsCommand" app/Modules/DotwAI/Providers/DotwAIServiceProvider.php shows registration
    - grep -n "clean-sessions" app/Console/Kernel.php shows scheduled entry with 03:00
    - grep -n "dotwai_agent\|action.*search" app/Modules/DotwAI/Config/dotwai-system-message.md shows single-tool description
    - grep -c "sessionContext\|next_actions\|stage" app/Modules/DotwAI/Config/dotwai-system-message.md returns value greater than or equal to 5
    - grep -c "APR\|non-refundable" app/Modules/DotwAI/Config/dotwai-system-message.md returns value greater than or equal to 2
  </acceptance_criteria>
  <done>System message updated for single-tool agent. CleanStaleSessionsCommand registered and scheduled daily at 03:00. All acceptance criteria verifiable by grep.</done>
</task>

<task type="auto">
  <name>Task 2: Replace n8n workflow JSON with single-tool Resayil workflow</name>
  <read_first>
    - public/downloads/api-templates/dotw-b2c-ai-agent-workflow.json (FULL file — read to understand n8n JSON structure before replacing)
    - app/Modules/DotwAI/Config/dotwai-system-message.md (just updated — embed its content as the systemMessage in the AI Agent node)
  </read_first>
  <files>
    public/downloads/api-templates/dotw-b2c-ai-agent-workflow.json
  </files>
  <action>
COMPLETELY REPLACE public/downloads/api-templates/dotw-b2c-ai-agent-workflow.json.

Read the existing file first to understand the n8n node JSON format. Then write a NEW workflow JSON with this structure. The new workflow has 7 nodes total: 3 main-flow nodes + 3 sub-nodes (LLM, memory, tool) + 1 config node.

Node specifications:

**Node 1 — Resayil Trigger** (receives WhatsApp messages, per D-22):
- type: "n8n-nodes-resayil.resayilTrigger"
- typeVersion: 1
- id: "resayil-trigger"
- name: "Resayil Trigger"
- position: [200, 400]
- parameters.deviceId: "68ac2c4c80090e92ccbf6d74"
- credentials.resayilApi: { id: "4QeuhC3zYnCEYxpd", name: "Resayil AI" }
- webhookId: "dotw-b2c-single-tool"

**Node 2 — DOTW Config** (Set node for configurable variables):
- type: "n8n-nodes-base.set"
- typeVersion: 3.4
- id: "dotw-config"
- name: "DOTW Config"
- position: [450, 400]
- parameters.assignments.assignments: two entries:
  1. { id: "config-base-url", name: "baseUrl", value: "https://development.citycommerce.group", type: "string" }
  2. { id: "config-telephone", name: "telephone", value: "={{ $('Resayil Trigger').item.json.data.fromNumber }}", type: "string" }
- notes: "Set baseUrl to your Laravel server URL. Change to production URL when deploying."

**Node 3 — DOTW Hotel Booking Agent** (AI Agent, per D-20):
- type: "@n8n/n8n-nodes-langchain.agent"
- typeVersion: 1.8
- id: "agent-main"
- name: "DOTW Hotel Booking Agent"
- position: [700, 400]
- parameters.options.systemMessage: [READ the full content of the UPDATED dotwai-system-message.md and embed it as a properly JSON-escaped string — all newlines as \n, all backticks as literal backticks (they are safe in JSON strings), no unescaped quotes]

**Node 4 — OpenAI Chat Model** (per D-17, D-18):
- type: "@n8n/n8n-nodes-langchain.lmChatOpenAi"
- typeVersion: 1.1
- id: "openai-chat-model"
- name: "OpenAI Chat Model (gpt-4o)"
- position: [500, 620]
- parameters.model: "gpt-4o"
- parameters.options.temperature: 0.3
- credentials.openAiApi: { id: "YOUR_OPENAI_CREDENTIAL_ID", name: "OpenAI API" }

**Node 5 — Window Buffer Memory** (per D-19):
- type: "@n8n/n8n-nodes-langchain.memoryBufferWindow"
- typeVersion: 1.3
- id: "window-buffer-memory"
- name: "Window Buffer Memory"
- position: [700, 620]
- parameters.sessionKey: "={{ $('DOTW Config').item.json.telephone }}"
- parameters.contextWindowLength: 20

**Node 6 — Single HTTP Tool: dotwai_agent** (per D-21 — ONE tool for all actions):
- type: "@n8n/n8n-nodes-langchain.toolHttpRequest"
- typeVersion: 1.1
- id: "tool-dotwai-agent"
- name: "Tool: dotwai_agent"
- position: [700, 800]
- parameters.name: "dotwai_agent"
- parameters.description: "The unified DOTW hotel booking agent tool. Use this for ALL operations: searching hotels, viewing room details, booking (locking rates), generating payment links, cancelling bookings, checking booking status, listing booking history, and resending vouchers. Send the action name and a params object. The response includes whatsappMessage (ready to send to the customer) and sessionContext (your current journey stage and next available actions — always follow next_actions to guide the customer)."
- parameters.method: "POST"
- parameters.url: "={{ $('DOTW Config').item.json.baseUrl }}/api/dotwai/agent-b2c"
- parameters.authentication: "genericCredentialType"
- parameters.genericAuthType: "httpHeaderAuth"
- parameters.sendHeaders: true
- parameters.headerParameters.parameters: [{ name: "Content-Type", value: "application/json" }, { name: "Accept", value: "application/json" }]
- parameters.sendBody: true
- parameters.specifyBody: "json"
- parameters.jsonBody: "={\n  \"telephone\": \"{{ $('DOTW Config').item.json.telephone }}\",\n  \"action\": \"{action}\",\n  \"params\": {params}\n}"
- parameters.placeholderDefinitions.values: two placeholders:
  1. { name: "action", description: "The operation to perform. One of: search (find hotels), details (get room details for a hotel — use option number), book (lock a rate — use option number), pay (generate payment link for current prebook), cancel (cancel booking — use confirm:no to preview, confirm:yes to execute), status (check booking status), history (list bookings with optional filters), voucher (resend booking confirmation voucher).", type: "string" }
  2. { name: "params", description: "JSON object with action-specific parameters. search: {city, check_in, check_out, occupancy: [{adults, children_ages}], hotel?, star_rating?, meal_type?, refundable?, price_min?, price_max?, nationality?}. details: {option: N}. book: {option: N}. pay: {}. cancel: {confirm: 'no'} or {confirm: 'yes', penalty_amount: X}. status: {} or {prebook_key: 'xxx'}. history: {status?, from_date?, to_date?, page?, per_page?}. voucher: {}.", type: "json" }

**Node 7 — Resayil Send** (per D-23):
- type: "n8n-nodes-resayil.resayil"
- typeVersion: 1
- id: "resayil-send"
- name: "Resayil Send"
- position: [950, 400]
- parameters.deviceId: "68ac2c4c80090e92ccbf6d74"
- parameters.to: "={{ $('Resayil Trigger').item.json.data.fromNumber }}"
- parameters.message: "={{ $json.output }}"
- credentials.resayilApi: { id: "4QeuhC3zYnCEYxpd", name: "Resayil AI" }

**Connections:**
```json
{
  "Resayil Trigger": {
    "main": [[{"node": "DOTW Config", "type": "main", "index": 0}]]
  },
  "DOTW Config": {
    "main": [[{"node": "DOTW Hotel Booking Agent", "type": "main", "index": 0}]]
  },
  "DOTW Hotel Booking Agent": {
    "main": [[{"node": "Resayil Send", "type": "main", "index": 0}]]
  },
  "OpenAI Chat Model (gpt-4o)": {
    "ai_languageModel": [[{"node": "DOTW Hotel Booking Agent", "type": "ai_languageModel", "index": 0}]]
  },
  "Window Buffer Memory": {
    "ai_memory": [[{"node": "DOTW Hotel Booking Agent", "type": "ai_memory", "index": 0}]]
  },
  "Tool: dotwai_agent": {
    "ai_tool": [[{"node": "DOTW Hotel Booking Agent", "type": "ai_tool", "index": 0}]]
  }
}
```

**Top-level workflow JSON:**
```json
{
  "name": "DOTW AI Hotel Booking Agent - B2C (Single Tool v2.0)",
  "nodes": [... all 7 nodes ...],
  "connections": {... as above ...},
  "settings": {"executionOrder": "v1"},
  "staticData": null,
  "meta": {
    "templateId": "dotw-b2c-ai-agent-single-tool-v2",
    "templateCredsSetupCompleted": false,
    "instanceId": ""
  },
  "tags": [
    {
      "createdAt": "2026-03-28T00:00:00.000Z",
      "updatedAt": "2026-03-28T00:00:00.000Z",
      "id": "dotw-hotel-booking",
      "name": "DOTW Hotel Booking"
    }
  ],
  "pinData": {},
  "_readme": {
    "title": "DOTW AI Hotel Booking Agent - B2C (Single Tool v2.0)",
    "version": "2.0.0",
    "created": "2026-03-28",
    "description": "Streamlined n8n AI Agent workflow using ONE HTTP tool (dotwai_agent) for all DOTW booking operations. Laravel manages per-phone session state so the AI only needs to send action + minimal params.",
    "setup": {
      "1_resayil": "Resayil nodes use device 68ac2c4c80090e92ccbf6d74 and credential ID 4QeuhC3zYnCEYxpd (name: Resayil AI). Update if your setup differs.",
      "2_openai": "Update YOUR_OPENAI_CREDENTIAL_ID in the OpenAI Chat Model node. Or swap the LLM node for an Ollama-compatible node pointing to llm.resayil.io for unlimited large model usage.",
      "3_base_url": "Set baseUrl in the DOTW Config node to your Laravel server URL.",
      "4_how_it_works": "Customer sends WhatsApp message -> Resayil Trigger -> DOTW Config (extracts phone) -> AI Agent -> dotwai_agent tool -> /api/dotwai/agent-b2c -> returns whatsappMessage + sessionContext -> AI forwards whatsappMessage -> Resayil Send -> customer.",
      "5_session": "Laravel tracks the customer's journey (search -> details -> prebook -> payment -> confirmation) per phone number. The AI uses sessionContext.next_actions to know what to offer next.",
      "6_model": "Default: gpt-4o, temperature 0.3. Change to any OpenAI-compatible model."
    }
  }
}
```

CRITICAL: Before writing the file, read the full updated dotwai-system-message.md and use its exact content as the systemMessage value in the AI Agent node. Properly JSON-escape the content: replace all newlines with \n, ensure no unescaped double-quotes within the markdown text content (escape them as \"). The backtick characters in code blocks are safe in JSON strings.

Verify the output is valid JSON by checking json_decode() would succeed.
  </action>
  <verify>
    <automated>cd "/c/Users/User/OneDrive - City Travelers/soud-laravel" && php -r "json_decode(file_get_contents('public/downloads/api-templates/dotw-b2c-ai-agent-workflow.json')); echo json_last_error() === JSON_ERROR_NONE ? 'VALID JSON' : 'INVALID: '.json_last_error_msg();"</automated>
  </verify>
  <acceptance_criteria>
    - php -r json_decode test on the file returns "VALID JSON" (no parse errors)
    - grep -c "resayil-trigger\|resayilTrigger" public/downloads/api-templates/dotw-b2c-ai-agent-workflow.json returns 1 or more (Resayil trigger node present)
    - grep -c "resayil-send\|resayil.*send\|Resayil Send" public/downloads/api-templates/dotw-b2c-ai-agent-workflow.json returns 1 or more (Resayil send node present)
    - grep -c "agent-b2c" public/downloads/api-templates/dotw-b2c-ai-agent-workflow.json returns 1 or more (HTTP tool points to agent-b2c)
    - grep -c "tool-dotwai-agent\|dotwai_agent" public/downloads/api-templates/dotw-b2c-ai-agent-workflow.json returns 1 or more (single tool present)
    - grep -c "search_hotels\|get_hotel_details\|prebook_hotel\|payment_link" public/downloads/api-templates/dotw-b2c-ai-agent-workflow.json returns 0 (old 11 tools removed)
    - grep -c "68ac2c4c80090e92ccbf6d74" public/downloads/api-templates/dotw-b2c-ai-agent-workflow.json returns 2 or more (device ID in both Resayil nodes)
    - grep -c "4QeuhC3zYnCEYxpd" public/downloads/api-templates/dotw-b2c-ai-agent-workflow.json returns 2 or more (credential ID in both Resayil nodes)
    - grep "contextWindowLength.*20\|contextWindowLength.*20" public/downloads/api-templates/dotw-b2c-ai-agent-workflow.json shows Window Buffer Memory has 20 message window
    - grep "fromNumber" public/downloads/api-templates/dotw-b2c-ai-agent-workflow.json shows Resayil Send reads fromNumber from trigger
  </acceptance_criteria>
  <done>n8n workflow JSON replaced with single-tool Resayil workflow. Valid JSON. Old 11 tool nodes removed. Resayil Trigger and Send nodes present. One dotwai_agent HTTP tool pointing to /api/dotwai/agent-b2c.</done>
</task>

</tasks>

<verification>
After both tasks complete:

```bash
# System message has single-tool content
grep -c "dotwai_agent" app/Modules/DotwAI/Config/dotwai-system-message.md

# Command is registered
php artisan list | grep clean-sessions

# Command runs cleanly
php artisan dotwai:clean-sessions --dry-run

# Workflow JSON is valid
php -r "json_decode(file_get_contents('public/downloads/api-templates/dotw-b2c-ai-agent-workflow.json')); echo json_last_error() === JSON_ERROR_NONE ? 'VALID' : 'INVALID';"

# Old tools are gone
grep -c "search_hotels\|prebook_hotel" public/downloads/api-templates/dotw-b2c-ai-agent-workflow.json

# Resayil nodes present
grep -c "resayil" public/downloads/api-templates/dotw-b2c-ai-agent-workflow.json

# Single agent tool present
grep -c "agent-b2c" public/downloads/api-templates/dotw-b2c-ai-agent-workflow.json
```
</verification>

<success_criteria>
- dotwai-system-message.md describes ONE tool (dotwai_agent) with all 8 actions in bilingual AR/EN, plus sessionContext usage guide
- dotw-b2c-ai-agent-workflow.json is valid JSON importable into n8n with: Resayil Trigger (device 68ac2c4c80090e92ccbf6d74), DOTW Config, AI Agent, OpenAI Chat Model, Window Buffer Memory (20 messages, phone session key), ONE HTTP tool (dotwai_agent -> /api/dotwai/agent-b2c), Resayil Send
- Old 11-tool structure replaced (no search_hotels, prebook_hotel, payment_link, etc. tool nodes)
- php artisan dotwai:clean-sessions --dry-run exits 0
- Command scheduled at 03:00 daily in Kernel.php
</success_criteria>

<output>
After completion, create .planning/phases/23-agent-facade-n8n-workflow/23-02-SUMMARY.md
</output>
