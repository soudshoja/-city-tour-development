---
phase: 24-dotw-certification-fixes-v2-olga-march-27-feedback
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - app/Modules/DotwAI/Services/BookingService.php
  - app/Modules/DotwAI/Services/HotelSearchService.php
  - app/Modules/DotwAI/Services/LifecycleService.php
  - app/Modules/DotwAI/Services/MessageBuilderService.php
  - app/Modules/DotwAI/Views/voucher-pdf.blade.php
  - app/Modules/DotwAI/Models/DotwAIBooking.php
  - app/Modules/DotwAI/Config/dotwai-system-message.md
  - app/Console/Commands/DotwCertify.php
autonomous: true
requirements: [CERT-04, CERT-05]

must_haves:
  truths:
    - "APR flow (savebooking + bookitinerary) is completely removed — all bookings use confirmBooking only"
    - "No is_apr checks gate booking behavior (auto-invoice, reminders, cancellation blocking)"
    - "Nationality and residence are collected from user input, not hardcoded to 66"
    - "DotwCertify test 16 (APR) is marked N/A with explanation that DOTW removed APRs"
  artifacts:
    - path: "app/Modules/DotwAI/Services/BookingService.php"
      provides: "APR flow removed, nationality/residence from input"
      contains: "confirmBooking"
    - path: "app/Modules/DotwAI/Services/LifecycleService.php"
      provides: "APR filter removed from reminder queries"
    - path: "app/Console/Commands/DotwCertify.php"
      provides: "Test 16 marked N/A"
      contains: "DOTW removed APRs"
  key_links:
    - from: "app/Modules/DotwAI/Services/BookingService.php"
      to: "app/Services/DotwService.php"
      via: "Always calls confirmBooking, never savebooking+bookitinerary"
      pattern: "confirmBooking"
---

<objective>
Remove the APR (Advance Purchase Rate) booking flow entirely since DOTW confirmed APRs are removed from their API, and wire nationality/residence collection through the booking pipeline instead of hardcoding 66 (Kuwait).

Purpose: DOTW confirmed "Please disregard APRs" — dead code must be removed. Nationality/residence must come from user for accurate rate pricing.
Output: Clean booking flow using only confirmBooking, nationality/residence passed through from user input.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-dotw-certification-fixes-v2-olga-march-27-feedback/24-CONTEXT.md
@.claude/skills/dotw-api/SKILL.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove APR flow from all DotwAI services</name>
  <files>
    app/Modules/DotwAI/Services/BookingService.php,
    app/Modules/DotwAI/Services/HotelSearchService.php,
    app/Modules/DotwAI/Services/LifecycleService.php,
    app/Modules/DotwAI/Services/MessageBuilderService.php,
    app/Modules/DotwAI/Views/voucher-pdf.blade.php,
    app/Modules/DotwAI/Models/DotwAIBooking.php,
    app/Modules/DotwAI/Config/dotwai-system-message.md,
    app/Console/Commands/DotwCertify.php
  </files>
  <read_first>
    app/Modules/DotwAI/Services/BookingService.php (full file — APR logic scattered throughout),
    app/Modules/DotwAI/Services/LifecycleService.php (lines 20-70 for APR filter in reminder/deadline queries),
    app/Modules/DotwAI/Services/HotelSearchService.php (lines 550-600 for is_apr detection),
    app/Modules/DotwAI/Services/MessageBuilderService.php (lines 295-310 for APR label, lines 645-660 for voucher APR),
    app/Modules/DotwAI/Views/voucher-pdf.blade.php (lines 160-175 for APR section),
    app/Modules/DotwAI/Models/DotwAIBooking.php (lines 70-110 for fillable/casts),
    app/Modules/DotwAI/Config/dotwai-system-message.md (lines 195-205 for APR section),
    app/Console/Commands/DotwCertify.php (lines 2656-2982 for test 16)
  </read_first>
  <action>
    **CERT-05: Remove APR Flow**

    Olga confirmed: "Please disregard APRs, these has been recently removed from our API." All APR-specific logic is now dead code.

    1. **BookingService.php** — Remove APR booking path:
       - Remove the `callDotwConfirmOrSave()` private method (around line 764-790) which branches on `$booking->is_apr` to use `savebooking + bookitinerary`. Replace all calls to it with a direct call to `$dotwService->confirmBooking(...)`.
       - In `confirmBooking()` method (around line 294-298): Remove the `if ($booking->is_apr)` block that calls `$this->invoiceAPRBooking($booking)`. All bookings now follow the standard reminder cycle.
       - In `confirmBookingAfterPayment()` method (around line 410-414): Same removal of APR auto-invoice block.
       - Remove the entire `invoiceAPRBooking()` private method (around line 458-480).
       - In `reblockAndConfirm()` (around line 660-668): Remove `'is_apr' => $allRestricted` from the returned array. The is_apr field will always be false.
       - In `prebook()` (around lines 155, 178): Change `'is_apr' => $blockedRoom['is_apr'] ?? false` to `'is_apr' => false` (or remove the line and let DB default handle it). Keep the column for now — removing the migration column is risky.

    2. **HotelSearchService.php** — Remove APR detection:
       - In `parseRoomDetails()` (around line 555-562): Remove the `$isAPR = false;` and the `$isAPR = true;` assignment block. Set `'is_apr' => false` in the output array (line 596).
       - In `applyPostSearchFilters()` (around line 464-476): The nonRefundable check can stay for display purposes (it tells the user the rate is non-refundable, even though it's not APR anymore). Keep `is_refundable` logic but remove any APR-specific branching.

    3. **LifecycleService.php** — Remove APR filter from queries:
       - In `getDueReminders()` (around line 46): Remove `->where('is_apr', false)`. All confirmed bookings get reminders now (nonrefundable rates may still exist, just not APR-specific).
       - Update PHPDoc (lines 20-21, 34) to remove APR references.
       - In `getPassedDeadlines()` (around line 61): Update comment to remove APR reference.

    4. **MessageBuilderService.php** — Remove APR labels:
       - In `formatPrebookConfirmation()` (around line 300-304): Change the APR label logic. Instead of:
         `$label = $isApr ? 'Non-Refundable (APR) | ...' : 'Non-Refundable | ...'`
         Simplify to: `$label = 'Non-Refundable | ghyr qabl llastyrdad'`
         Remove the `$isApr` variable.
       - In `formatVoucherMessage()` (around line 650-658): Remove `$isApr` variable and the APR-specific label. Change `"Non-Refundable (APR) | ghyr qabl llastyrdad"` to `"Non-Refundable | ghyr qabl llastyrdad"`.

    5. **voucher-pdf.blade.php** — Remove APR section:
       - Around line 168-170: Change `@elseif(!($booking->is_refundable ?? true) || ($booking->is_apr ?? false))` to `@elseif(!($booking->is_refundable ?? true))`
       - Change "Non-Refundable (APR)" text to "Non-Refundable" (line 170).

    6. **DotwAIBooking.php** — Keep `is_apr` in fillable/casts for backward compatibility but it will always be false. No changes needed.

    7. **dotwai-system-message.md** — Remove APR section:
       - Remove lines 197-201 (the APR Rates section header and content).
       - Replace with: "Note: All rates follow the standard cancellation policy. Non-refundable rates cannot be cancelled but are not APR (Advance Purchase Rates have been removed by DOTW)."

    8. **DotwCertify.php** — Mark test 16 as N/A:
       - In `runTest16()` method: Replace the entire method body with:
         ```php
         $this->startTest(16, 'APR Booking — REMOVED BY DOTW');
         $this->skipTest(16, 'DOTW removed APRs from API (confirmed by Olga Chicu, March 2026). savebooking + bookitinerary flow is no longer applicable. All bookings use confirmbooking.');
         ```
  </action>
  <verify>
    <automated>cd "C:/Users/User/OneDrive - City Travelers/soud-laravel" && echo "=== No savebooking in BookingService ===" && grep -c "savebooking\|bookitinerary" app/Modules/DotwAI/Services/BookingService.php && echo "=== No invoiceAPRBooking ===" && grep -c "invoiceAPRBooking" app/Modules/DotwAI/Services/BookingService.php && echo "=== Test 16 skipped ===" && grep "DOTW removed APRs" app/Console/Commands/DotwCertify.php && echo "=== No APR in LifecycleService filter ===" && grep -c "is_apr" app/Modules/DotwAI/Services/LifecycleService.php</automated>
  </verify>
  <acceptance_criteria>
    - app/Modules/DotwAI/Services/BookingService.php does NOT contain `savebooking` or `bookitinerary` method calls
    - app/Modules/DotwAI/Services/BookingService.php does NOT contain `invoiceAPRBooking` method
    - app/Modules/DotwAI/Services/LifecycleService.php does NOT contain `where('is_apr', false)` filter
    - app/Console/Commands/DotwCertify.php runTest16 contains `DOTW removed APRs`
    - app/Modules/DotwAI/Services/MessageBuilderService.php does NOT contain `Non-Refundable (APR)`
    - app/Modules/DotwAI/Views/voucher-pdf.blade.php does NOT contain `Non-Refundable (APR)`
    - app/Modules/DotwAI/Config/dotwai-system-message.md does NOT contain `APR Rates (Non-Refundable)` section header
  </acceptance_criteria>
  <done>All APR-specific code paths removed. BookingService uses confirmBooking only. Test 16 marked N/A. LifecycleService treats all bookings equally. WhatsApp messages and vouchers say "Non-Refundable" without "(APR)".</done>
</task>

<task type="auto">
  <name>Task 2: Wire nationality/residence from user input through booking pipeline</name>
  <files>
    app/Modules/DotwAI/Services/BookingService.php,
    app/Modules/DotwAI/Services/HotelSearchService.php
  </files>
  <read_first>
    app/Modules/DotwAI/Services/HotelSearchService.php (lines 210-225 for getHotelDetails residenceCode hardcode, lines 56-106 for searchHotels nationality resolution),
    app/Modules/DotwAI/Services/BookingService.php (lines 545-570 for reblockForFreshAllocation residenceCode hardcode, lines 575-610 for buildRoomSelections)
  </read_first>
  <action>
    **CERT-04: Nationality/Residence Collection**

    Currently residence is hardcoded to `config('dotwai.default_residence', '66')` (Kuwait) everywhere. Nationality uses fuzzy matching in HotelSearchService but residence does not. Fix both to accept user input.

    1. **HotelSearchService.php**:
       - In `searchHotels()` method (around line 55-60): The `resolveNationalityCode()` call already handles nationality from input. Add the same for residence:
         ```php
         $residenceCode = $this->resolveResidenceCode($input['residence'] ?? null);
         ```
       - In `getHotelDetails()` method (around line 214): Change:
         `$residenceCode = config('dotwai.default_residence', '66');`
         To:
         `$residenceCode = $this->resolveResidenceCode($input['residence'] ?? null);`
       - Add a new private method `resolveResidenceCode()` that mirrors `resolveNationalityCode()`:
         ```php
         private function resolveResidenceCode(?string $residence): string
         {
             if (empty($residence)) {
                 return config('dotwai.default_residence', '66');
             }
             $country = $this->fuzzyMatcher->resolveCountry($residence);
             return $country ? (string) $country->code : config('dotwai.default_residence', '66');
         }
         ```

    2. **BookingService.php**:
       - In `reblockForFreshAllocation()` (around line 550-551): Change the hardcoded:
         ```php
         'nationality_code' => config('dotwai.default_nationality', '66'),
         'residence_code'   => config('dotwai.default_residence', '66'),
         ```
         To use the booking's stored codes:
         ```php
         'nationality_code' => $booking->nationality_code ?? config('dotwai.default_nationality', '66'),
         'residence_code'   => $booking->residence_code ?? config('dotwai.default_residence', '66'),
         ```
       - Same fix at the second occurrence around lines 564-565.
       - In `buildRoomSelections()` (around line 580-581): Already reads from `$roomData` which is correct. No change needed — the fix flows from the upstream callers passing correct data.

    3. Keep `config('dotwai.default_residence', '66')` as the FALLBACK for when user doesn't provide residence (WhatsApp flow may not always collect it). The default stays 66 (Kuwait) since that's where the agency operates.
  </action>
  <verify>
    <automated>cd "C:/Users/User/OneDrive - City Travelers/soud-laravel" && grep -n "resolveResidenceCode" app/Modules/DotwAI/Services/HotelSearchService.php && grep -n "booking->nationality_code\|booking->residence_code" app/Modules/DotwAI/Services/BookingService.php | head -5</automated>
  </verify>
  <acceptance_criteria>
    - app/Modules/DotwAI/Services/HotelSearchService.php contains `resolveResidenceCode` method definition
    - app/Modules/DotwAI/Services/HotelSearchService.php getHotelDetails calls `resolveResidenceCode` instead of hardcoded config
    - app/Modules/DotwAI/Services/BookingService.php reblockForFreshAllocation uses `$booking->nationality_code` and `$booking->residence_code` with config fallback
    - The hardcoded `config('dotwai.default_residence', '66')` is NOT the only source of residence in search/booking paths
  </acceptance_criteria>
  <done>Nationality and residence flow from user input through HotelSearchService (fuzzy resolved) and BookingService (from stored booking data), with config default 66 as fallback only.</done>
</task>

</tasks>

<verification>
- `grep -rn "savebooking\|bookItinerary\|invoiceAPRBooking" app/Modules/DotwAI/Services/BookingService.php` returns 0 lines
- `grep -rn "is_apr.*true\|is_apr.*=.*true" app/Modules/DotwAI/Services/` returns 0 lines (no code sets is_apr to true)
- `grep -c "resolveResidenceCode" app/Modules/DotwAI/Services/HotelSearchService.php` returns at least 3 (definition + 2 calls)
- `php artisan tinker --execute="echo 'syntax ok';"` exits 0 (no parse errors)
</verification>

<success_criteria>
1. APR flow completely removed: no savebooking, no bookitinerary, no invoiceAPRBooking
2. All bookings use confirmBooking regardless of rate type
3. DotwCertify test 16 marked as N/A with clear explanation
4. LifecycleService sends reminders for all confirmed bookings (no APR exclusion)
5. Nationality resolved via fuzzy matching in both search and details
6. Residence resolved via fuzzy matching (new resolveResidenceCode method), fallback to config 66
</success_criteria>

<output>
After completion, create `.planning/phases/24-dotw-certification-fixes-v2-olga-march-27-feedback/24-02-SUMMARY.md`
</output>
