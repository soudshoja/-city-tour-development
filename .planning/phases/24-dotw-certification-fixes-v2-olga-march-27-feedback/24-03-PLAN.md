---
phase: 24-dotw-certification-fixes-v2-olga-march-27-feedback
plan: 03
type: execute
wave: 2
depends_on: [24-01, 24-02]
files_modified:
  - app/Modules/DotwAI/Services/MessageBuilderService.php
  - app/Modules/DotwAI/Services/HotelSearchService.php
  - app/Console/Commands/DotwCertify.php
autonomous: true
requirements: [CERT-06, CERT-07]

must_haves:
  truths:
    - "WhatsApp search results show cancellation policy, tariff notes, min stay, MSP, specials, taxes before booking"
    - "WhatsApp booking confirmation and voucher show all mandatory features post-booking"
    - "Special requests are shown in confirmation and voucher messages"
    - "DotwCertify has a 2-room booking + cancellation test with evidence capture"
    - "MSP is propagated from searchhotels to getRooms output and displayed in messages"
  artifacts:
    - path: "app/Modules/DotwAI/Services/MessageBuilderService.php"
      provides: "All mandatory DOTW features formatted in WhatsApp messages"
      contains: "Tariff Notes"
    - path: "app/Console/Commands/DotwCertify.php"
      provides: "2-room cancellation test"
      contains: "2-room"
  key_links:
    - from: "app/Modules/DotwAI/Services/MessageBuilderService.php"
      to: "app/Modules/DotwAI/Services/HotelSearchService.php"
      via: "Room data with all mandatory fields"
      pattern: "tariff_notes.*cancellation_rules.*specials"
    - from: "app/Console/Commands/DotwCertify.php"
      to: "app/Services/DotwService.php"
      via: "2-room search, block, confirm, cancel flow"
      pattern: "rooms no.*2"
---

<objective>
Wire all mandatory DOTW display features into WhatsApp messages (pre-booking and post-booking) and add a 2-room cancellation certification test.

Purpose: DOTW certification requires all features (cancellation policy, tariff notes, min stay, MSP, specials, special requests, taxes) shown before booking AND in confirmation/voucher. Olga also requires 2-room cancellation evidence.
Output: Complete WhatsApp message formatting with all mandatory features; DotwCertify 2-room cancel test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-dotw-certification-fixes-v2-olga-march-27-feedback/24-CONTEXT.md
@.planning/phases/24-dotw-certification-fixes-v2-olga-march-27-feedback/24-01-SUMMARY.md
@.planning/phases/24-dotw-certification-fixes-v2-olga-march-27-feedback/24-02-SUMMARY.md
@.claude/skills/dotw-api/SKILL.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire all mandatory display features into WhatsApp messages</name>
  <files>
    app/Modules/DotwAI/Services/MessageBuilderService.php,
    app/Modules/DotwAI/Services/HotelSearchService.php
  </files>
  <read_first>
    app/Modules/DotwAI/Services/MessageBuilderService.php (full file — all format methods need review),
    app/Modules/DotwAI/Services/HotelSearchService.php (lines 520-610 for parseRoomDetails, lines 440-500 for applyPostSearchFilters — MSP propagation)
  </read_first>
  <action>
    **CERT-07: Mandatory Display Features in WhatsApp**

    Olga requires ALL of these shown BEFORE booking AND in confirmation/voucher:
    1. Cancellation Policy (rules with dates/charges)
    2. Tariff Notes
    3. Minimum Stay
    4. MSP (Minimum Selling Price)
    5. Special Promotions
    6. Special Requests (selected ones)
    7. Restricted Cancellation Rules (cancelRestricted/amendRestricted)
    8. Taxes and Property Fees

    **A. Fix MSP propagation (HotelSearchService.php):**
    - In `parseRoomDetails()` around line 544: `$msp = 0; // MSP not directly in getRooms parsed output`
    - The MSP IS returned by DOTW in getRooms as `<totalMinimumSelling>` per rateBasis. DotwService already parses it in `parseGetRoomsResponseV2()` but under `'totalMinimumSelling'` key in the roomType detail.
    - However, `parseRoomDetails()` receives data from `parseGetRoomsResponseV2()` where the detail has no `totalMinimumSelling` — it's under `$detail` but not propagated.
    - Check DotwService `parseGetRoomsResponseV2()` (around line 1853): it returns `'totalMinimumSelling' => ...` in roomTypes but NOT in the details array.
    - The browse-mode parse (`parseGetRoomsResponse()` around line 1810) has a different structure. The `details` array includes `id`, `status`, `price`, `taxes`, `allocationDetails`, etc. — but NOT MSP.
    - Fix: In DotwService `parseGetRoomsResponse()` (around line 1853-1869), add `'totalMinimumSelling'` to each detail entry:
      Actually, MSP is at the rateBasis level. Look at how DotwService parses it — `$rateBasis->totalMinimumSelling`. Add it to the details array.
    - Then in HotelSearchService `parseRoomDetails()`, read it: `$msp = (float) ($detail['totalMinimumSelling'] ?? 0);`
    - Add `'minimum_selling_price' => $msp` to the room output array.

    **B. Enhance formatSearchResults (MessageBuilderService.php):**
    - The `formatSearchResults()` method (line 51) already shows basic info per hotel (name, star, price, meal type).
    - It shows summary-level data (cheapest price, refundable status). For search results listing, keep it brief — the mandatory features are shown in DETAIL view (formatRoomDetails / formatPrebookConfirmation).
    - No changes needed for the initial hotel list.

    **C. Enhance room detail view (MessageBuilderService.php):**
    - Currently `formatRoomDetails()` or equivalent displays room options. Need to ensure each room shows:

    Add a new static method `formatMandatoryFeatures(array $room): string` that formats:
    ```
    -- Cancellation Policy --
    Free cancellation until {date}
    After {date}: {charge} {currency} penalty
    {if cancelRestricted}: Cancellation restricted during {fromDate} to {toDate}

    -- Tariff Notes --
    {tariffNotes text}

    -- Minimum Stay --
    Minimum {N} nights required

    -- Taxes & Fees --
    {foreach propertyFee}: {name}: {amount} {currency} {included/excluded}

    -- Special Promotions --
    {foreach special}: {name}: {description}

    -- Minimum Selling Price --
    Min. selling price: {MSP} {currency}
    ```

    Wire this method into:
    1. `formatPrebookConfirmation()` (line 286) — add mandatory features section BEFORE the "confirm?" prompt. Call `formatMandatoryFeatures($prebook)` and append to the message.
    2. `formatBookingConfirmation()` (line 343) — add mandatory features after booking details.
    3. `formatVoucherMessage()` (line 595) — add mandatory features section in the voucher.

    **D. Add special requests to confirmation/voucher:**
    - In `formatBookingConfirmation()` and `formatVoucherMessage()`: if `$booking->special_requests` (or `$confirmation['special_requests']`) is not empty, add:
      ```
      -- Special Requests --
      - Non-smoking room
      - Early check-in
      ```
    - Map codes to labels using `config('dotwai.special_request_codes')`.

    **E. Show restricted cancellation warning:**
    - In `formatMandatoryFeatures()`, check cancellation_rules for `cancelRestricted: true` entries and add:
      ```
      WARNING: Cancellation restricted {fromDate} - {toDate}
      ```

    **F. Bilingual formatting:**
    - All labels should be bilingual (English | Arabic) consistent with existing messages.
    - Example: `Cancellation Policy | siasat alalghaa'`
  </action>
  <verify>
    <automated>cd "C:/Users/User/OneDrive - City Travelers/soud-laravel" && grep -n "formatMandatoryFeatures\|Tariff Notes\|Minimum Stay\|Special Promotions\|Taxes.*Fees\|Special Requests\|cancelRestricted\|minimumSelling\|minimum_selling_price" app/Modules/DotwAI/Services/MessageBuilderService.php | head -20</automated>
  </verify>
  <acceptance_criteria>
    - MessageBuilderService.php contains a `formatMandatoryFeatures` method (or equivalent inline blocks)
    - MessageBuilderService.php `formatPrebookConfirmation` includes cancellation policy, tariff notes, min stay, taxes
    - MessageBuilderService.php `formatBookingConfirmation` includes all mandatory features
    - MessageBuilderService.php `formatVoucherMessage` includes all mandatory features
    - MessageBuilderService.php maps special request codes to labels using config
    - HotelSearchService.php `parseRoomDetails` includes `minimum_selling_price` in output
    - Cancellation restricted rules are displayed with warning
  </acceptance_criteria>
  <done>All 8 mandatory DOTW display features formatted in WhatsApp messages: pre-booking (prebook confirmation), post-booking (booking confirmation), and voucher. MSP propagated from DOTW API through to display.</done>
</task>

<task type="auto">
  <name>Task 2: Add 2-room cancellation test to DotwCertify</name>
  <files>
    app/Console/Commands/DotwCertify.php
  </files>
  <read_first>
    app/Console/Commands/DotwCertify.php (lines 2120-2250 for test 7 which is the existing single-room cancel with productsLeftOnItinerary — use as template, lines 1560-1700 for existing multi-room blocking/confirm patterns)
  </read_first>
  <action>
    **CERT-06: 2-Room Cancellation Test**

    Olga requires evidence of booking 2 rooms then cancelling. Create a new test (test 20 or next available number) that:

    1. Search hotels in Dubai for 2 rooms (2 adults each), dates 90+ days out
    2. getRooms browse for the found hotel
    3. getRooms blocking for 2 rooms — pass 2 rooms in the blocking request with roomTypeSelected
    4. confirmBooking with 2 rooms, 2 passengers (1 per room)
    5. cancelBooking step 1 (confirm=no) — get charge, verify 2 services returned
    6. cancelBooking step 2 (confirm=yes) — pass penaltyApplied for EACH service, verify productsLeftOnItinerary = 0

    Implementation details:
    - Add method `runTest20(): void` (or next available test number after existing tests).
    - Follow the exact pattern of test 7 (single-room cancel) but with 2 rooms.
    - Key differences from test 7:
      a. `<rooms no="2">` with 2 `<room>` elements in searchhotels and getrooms
      b. In getRooms blocking, 2 `<room>` elements each with `<roomTypeSelected>`
      c. In confirmBooking, 2 rooms with passengers
      d. In cancelBooking confirm=yes, `<testPricesAndAllocation>` must include `<service>` entries for EACH room/service reference number
    - For the cancel confirmation step, iterate over all `<service>` elements in the charge response and pass each with its `<penaltyApplied>`.
    - Check `productsLeftOnItinerary` in the final cancel response — should be 0 (all rooms cancelled).
    - Log all XML request/response pairs for certification evidence.

    - Register the test in the `handle()` method's test dispatch list (the `match($test)` or switch statement).
    - Add it to the test count and description output.

    Use salutation codes from the fixed salutation map (`$this->state['salutationMap']`):
    - Room 1: Mr (147) + Mrs (149)
    - Room 2: Mr (147)

    Use the fixed `rateBasis = -1` (not 0) for all room elements.
  </action>
  <verify>
    <automated>cd "C:/Users/User/OneDrive - City Travelers/soud-laravel" && grep -n "2-room\|runTest20\|rooms no=\"2\"" app/Console/Commands/DotwCertify.php | head -10</automated>
  </verify>
  <acceptance_criteria>
    - app/Console/Commands/DotwCertify.php contains `runTest20` method (or equivalent test number)
    - The test method contains `rooms no="2"` in searchhotels XML
    - The test method contains `<confirm>yes</confirm>` cancelBooking step
    - The test method checks `productsLeftOnItinerary` in cancel response
    - The test is registered in the handle() test dispatch (runnable via `--test=20`)
    - All steps logged with step IDs (20a, 20b, 20c, etc.) for certification evidence
  </acceptance_criteria>
  <done>DotwCertify has a complete 2-room search-block-confirm-cancel test case that produces full XML logs for Olga's certification evidence.</done>
</task>

</tasks>

<verification>
- `grep -c "formatMandatoryFeatures\|Mandatory Features" app/Modules/DotwAI/Services/MessageBuilderService.php` returns at least 4
- `grep -c "runTest20" app/Console/Commands/DotwCertify.php` returns at least 2 (definition + dispatch)
- `grep "rooms no" app/Console/Commands/DotwCertify.php | grep "2"` returns the 2-room test XML
- `php artisan tinker --execute="echo 'syntax ok';"` exits 0
</verification>

<success_criteria>
1. All 8 mandatory features displayed in WhatsApp prebook, confirmation, and voucher messages
2. MSP propagated from DOTW API response through HotelSearchService to MessageBuilderService
3. Special request labels resolved from config codes to display names
4. Restricted cancellation rules shown with warning before booking
5. DotwCertify 2-room cancellation test runs search-block-confirm-cancel with full XML logging
6. productsLeftOnItinerary verified in 2-room cancel response
</success_criteria>

<output>
After completion, create `.planning/phases/24-dotw-certification-fixes-v2-olga-march-27-feedback/24-03-SUMMARY.md`
</output>
